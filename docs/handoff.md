# Phoenix Platform — Handoff para nueva sesión

> Leer este archivo primero al retomar el proyecto. Luego leer `progress.md` y `decisions.md`.

---

## Fuente de verdad del proyecto

Estos cuatro archivos son la fuente de verdad. Leerlos al inicio de cada sesión:

1. `docs/handoff.md` — contexto general y protocolo de trabajo
2. `docs/progress.md` — estado actual e hitos completados
3. `docs/decisions.md` — decisiones de arquitectura tomadas
4. `docs/backlog.md` — ideas registradas, pendientes de decisión

**Regla fundamental:** Si la documentación y la memoria del chat se contradicen, prevalece siempre la documentación del repositorio.

---

## Protocolo de trabajo

### Al iniciar una sesión
1. Leer `docs/handoff.md`
2. Leer `docs/progress.md`
3. Leer `docs/decisions.md`

### Al finalizar un hito
1. Actualizar la documentación únicamente si hubo cambios.
2. Hacer commit y push.

### Al completar un componente grande
Antes de comenzar el siguiente componente, crear obligatoriamente:
`docs/components/<nombre-del-componente>.md`

Componentes grandes: Phoenix Core, Execution Gateway, Auditor, Market Regime Engine, Portfolio Orchestrator, Bot SDK, Dashboard, cada bot aprobado.

El cierre debe incluir: nombre/versión, estado, objetivo, funcionalidades, interfaces, archivos, tests, decisiones, limitaciones, backlog, commit/tag y próximo componente. Actualizar también `docs/progress.md` y `docs/handoff.md`.

El documento del componente debe terminar siempre con una sección `## Estado` que lo marque como CONGELADO.

### Componentes congelados

Un componente marcado como **CONGELADO** no debe modificarse salvo para:

- Corrección de bugs.
- Problemas de seguridad.
- Refactorizaciones que no alteren el comportamiento.
- Cambios aprobados explícitamente.

Antes de modificar un componente congelado, documentar la decisión en `docs/decisions.md` y actualizar el documento del componente con el motivo del cambio. No agregar nuevas funcionalidades directamente sobre un componente congelado.

---

## ¿Qué es este proyecto?

Phoenix Platform es una plataforma modular de trading algorítmico. Está siendo construida desde cero con contratos inmutables y arquitectura por capas.

---

## Estado al 2026-08-20

- **Tag activo:** `v0.1.0`
- **Tests:** 5898 passing, 0 failing
- **Python:** 3.14 en local (requisito mínimo: 3.12)
- **Railway:** servicio `phoenix-smoke-demo` **eliminado manualmente** por el usuario tras la validación exitosa del Hito 3.68 (confirmado explícitamente en sesión, 2026-08-12) — ya no existe y no puede disparar autodeploys desde `main`; `railway.toml` (build + start command + `restartPolicyType = "NEVER"`) permanece en el repo como configuración de referencia para un futuro redeploy manual, sin servicio activo asociado
- **GitHub:** `garciarjosefina/phoenix-platform`, rama `main`
- **Fase activa:** Fase 3 — Execution Gateway (Auditorías Retrospectivas A, B y C cerradas; Hito 3.65: carga de `BybitDemoExecutionConfig` desde entorno; Hito 3.66: bootstrap integral; Hito 3.67: `smoke_test_bybit_demo_connection` de sólo lectura; Hito 3.68A: runner one-shot; Hito 3.69: `pyproject.toml` corregido y `railway.toml` mínimo; corrección post-3.69: `restartPolicyType = "NEVER"` explícito; **Hito 3.68 — ACEPTADO: primera ejecución real del smoke test desde Railway contra Bybit Demo, éxito verificado con `server_time` auténtico** — frontera de validación: autenticación/firma/headers/conectividad/lectura validados en producción; **Hito 3.70: `PositionsReader`/`query_bybit_demo_positions` — primera lectura de estado real (posiciones) vía Port separado del de ejecución (`ADR-002`)**; **corrección post-3.70: fail-closed ante paginación, campos accesorios (`leverage`/`unrealized_pnl`) opcionales sin abortar el snapshot, garantía conductual de ausencia de caché en `PositionsReader`, `PositionsSnapshot` declarado explícitamente observacional (no operativo) — cierra los 3 hallazgos IMPORTANTES de la auditoría independiente**; **Hito 3.71: `OpenOrdersReader`/`query_bybit_demo_open_orders` — segunda primitiva del read-side (`GET /v5/order/realtime`), identidad dual orderLinkId/orderId preservada, fail-closed ante paginación y no-caché incluidos desde el diseño inicial (lecciones de la corrección post-3.70 aplicadas proactivamente, no descubiertas por auditoría)**; **corrección post-3.71: `price="0"` tratado igual que `price=""` (→ `None`) sin abortar el snapshot, estado `"Triggered"` preservado como `"triggered"` en vez de rechazado — cierra los 2 hallazgos IMPORTANTES de la auditoría independiente**; **Hito 3.71 — ACEPTADO: reauditoría corta y focalizada post-corrección confirmó ambos hallazgos IMPORTANTES cerrados sin tolerancia excesiva ni regresión (21/21 probes conductuales, 8/8 mutaciones de reinyección detectadas)**; **Hito 3.72: `WalletBalanceReader`/`query_bybit_demo_wallet_balance` — tercera primitiva del read-side (`GET /v5/account/wallet-balance`), distinción explícita balance/equity/capital disponible, balance por moneda junto a totales de cuenta en un mismo snapshot, deuda MENOR de cierre de `Response` heredada de 3.70 cerrada sin cambiar producción**; **corrección post-3.72: auditoría adversarial clasificó `CORREGIR HITO 3.72` con 2 hallazgos IMPORTANTES (clasificación explícita de los 5 totales de cuenta contra evidencia oficial, sin relajar `""`→`None` sin evidencia field-specific; semántica corregida de `total_available_balance` — no es buying power en USDT) y 5 MENORES, todos cerrados con correcciones documentales y 30 tests nuevos, sin cambio de comportamiento productivo**; **Hito 3.72 — ACEPTADO: reauditoría focalizada post-corrección confirmó ambos hallazgos IMPORTANTES y los 5 MENORES cerrados sin regresión, política fail-closed de los cinco account-wide validada como aceptable, advertencia de no-atomicidad agregada a ADR-002**; **Hito 3.73: `InstrumentMetadataReader`/`query_bybit_demo_instrument_metadata` — cuarta primitiva del read-side (`GET /v5/market/instruments-info`), primera que toma un parámetro (`symbol`) y primera pública/sin autenticación — nueva primitiva `BybitPublicGetRequestSender`/`BybitPublicGetApi` paralela a la privada (GET stack de transporte reutilizado sin cambios, autenticación deliberadamente no reutilizada), composición sin credenciales Demo, identidad de instrumento fail-closed (cardinalidad + símbolo remoto verificado)**; **corrección post-3.73: auditoría adversarial clasificó `CORREGIR HITO 3.73` con 1 hallazgo IMPORTANTE (mapping `baseCoin`/`quoteCoin`/`settleCoin` ya era correcto en producción — hallazgo de cobertura, no de comportamiento; fixtures previos usaban `quoteCoin==settleCoin` idénticos, ocultando un cruce posible entre esos dos campos) y 2 MENORES, corregidos exclusivamente con 5 tests nuevos y documentación de metadata excluida en ADR-002 — cero cambios de producción, verificado byte-idéntico contra el commit auditado — **Hito 3.73 — ACEPTADO: reauditoría adversarial independiente posterior confirmó ambos hallazgos cerrados**; **Hito 3.74: `ExchangeStateSnapshot`/`CompositeExchangeStateReader` — primer primitivo de agregación del read-side (quinto Port, `ExchangeStateReader`), combina Positions+Open Orders+Wallet Balance (Opción A elegida explícitamente por el usuario tras STOP arquitectónico — Instrument Metadata queda fuera del agregado), `ObservationWindow` mide y expone drift temporal remoto sin decidir tolerancia, fail-closed ante fallo parcial sin ningún `try`/`except` nuevo, sin caché entre rondas, 14/16 mutaciones aplicables detectadas**; **corrección post-3.74: auditoría adversarial clasificó `CORREGIR HITO 3.74` con 1 hallazgo IMPORTANTE de cobertura (coherencia de configuración entre los tres sub-readers — producción ya era correcta, sin cambios), cerrado exclusivamente con 9 tests conductuales**; **Hito 3.74 — ACEPTADO: reauditoría adversarial final confirmó producción byte-idéntica por tree-hash de git, 8/8 mutaciones M1-M8 detectadas de forma independiente, config-load-once reverificado, 16/16 probes de regresión limpios**; creación/cancelación de órdenes siguen validadas sólo por tests, sin conexión real; **Hito 3.75 — Project Memory & Architecture Hardening (documental): `AGENTS.md`/`docs/architecture.md`/`ADR-003` creados (commit `75f66b3`); auditoría documental independiente clasificó `CORREGIR HITO 3.75` (hallazgo IMPORTANTE autorreferencial: el propio estado de aceptación de 3.75 no estaba registrado en el repo, más 4 MENORES de wording/deudas); corrección post-3.75 aplicada (commit `6620340`); reauditoría final independiente confirmó cadena de custodia, producción/tests byte-idénticos, los cinco hallazgos cerrados, prueba de reentrada ciega exitosa sin depender del chat en ningún paso — **`ACEPTAR HITO 3.75`**, ver `docs/progress.md` para el detalle** — **Hito 3.75 es ahora el último hito ACEPTADO; Project Memory & Architecture Hardening queda cerrado**; **Hito 3.76 — `ExpectedExecutionState`: lenguaje de dominio canónico e inmutable para "qué estado de ejecución debería existir" dentro de un scope explícito** (`expected_execution_state_contracts.py`: `ExpectedExecutionScope`/`ExpectedPosition`/`ExpectedOpenOrder`/`ExpectedExecutionState`) — NO construye el Reconciliation Engine, sólo resuelve "qué significa expected state" (primer punto sin decidir de `docs/architecture.md` §12); scope vacío aceptado por precedente uniforme del read-side (colección vacía = legítima, nunca error); identidad `order_id` es Phoenix, nunca exchange; `price` sigue el precedente observacional de `ExecutionOpenOrder`/3.71, no el acoplamiento estricto del write-side; sin acoplamiento a `ExchangeStateSnapshot` (sin `reconcile`/`compare`, verificado por AST); 118 tests nuevos, suite 5659→5777, 15/15 mutaciones M1-M15 detectadas; auditoría adversarial independiente clasificó `CORREGIR HITO 3.76` — IMPORTANTE-1 de cobertura: producción ya preserva `symbol`/`order_id` como identidad de string exacta (sin normalización silenciosa), pero 4 mutantes de normalización sobrevivían; **corrección post-3.76 aplicada (commit `ee88b3e`)**: 19 tests conductuales nuevos fijan identidad exacta, 9/9 mutaciones adicionales (M19/M20/M21/M28 + M31-M35) detectadas, cero cambios de producción; MENOR-1 (semántica de `price` bajo-especificada) y MENOR-3 (ausencia de timestamp y de identidad de cuenta simétrica) registrados como deuda explícita para el Hito 3.77, sin resolver; **Hito 3.76 — ACEPTADO: reauditoría adversarial final confirmó producción byte-idéntica por tree-hash de git, 13/13 mutaciones reinyectadas de forma independiente detectadas, 35/35 sondas propias limpias — registrado formalmente al iniciar el Hito 3.77 (veredicto alcanzado en la sesión de la reauditoría, nunca antes escrito al repo)**; **Hito 3.76 fue el último hito ACEPTADO**; **Hito 3.77 — Reconciliation Engine V1, Detection & Classification puro (sin repair)**: `reconcile_execution_state(*, expected, observed) -> ReconciliationResult` (`reconciliation_engine.py`), función pura sin Port/I/O; once tipos de divergencia sin strings libres (`reconciliation_contracts.py`); identidad de posiciones `(symbol, side)`, identidad de órdenes exclusivamente `order_id` Phoenix (`exchange_order_id` nunca fallback); regla identity-first/scope-second para órdenes; cierra la deuda de `price` de 3.76 con precondición fail-closed para LIMIT sin precio y MARKET nunca reconciliado; `ObservationWindow` preservada sin decidir freshness; account identity sigue como deuda simétrica abierta; 102 tests nuevos, suite 5796→5898, 24 mutaciones especificadas; **auditoría adversarial independiente clasificó `CORREGIR HITO 3.77`** con dos hallazgos IMPORTANTES y uno MENOR: IMPORTANTE-1 crítico — ni `PositionsSnapshot` ni `OpenOrdersSnapshot` exigen unicidad de identidad del lado observado, y el motor perdía silenciosamente una entidad real duplicada vía su diccionario de matching (podía producir `is_in_sync=True` con una orden/posición real sin contabilizar); IMPORTANTE-2 — la propagación de `observation_window` por identidad de objeto ya era correcta en producción pero sin cobertura de test que atravesara la función pública; MENOR-1 — coexistencia `OrderTypeMismatch`/`OrderPriceMismatch` defendible pero decidida sin el protocolo de STOP; la auditoría también corrigió un error de la batería de mutación original (M9 mal construido por el implementador — mutaba `order.symbol` en vez de `observed_order.symbol` — la mutación correcta sí es detectada, 24/24 sin equivalentes); **corrección post-3.77 aplicada**: dos precondiciones nuevas en `reconciliation_engine.py` (`_reject_duplicate_observed_position_identities`/`_reject_duplicate_observed_order_ids`) fallan cerrado ante cualquier identidad observada duplicada, exclusivamente dentro de 3.77 (`PositionsSnapshot`/`OpenOrdersSnapshot`/`ExecutionPosition`/`ExecutionOpenOrder` de 3.70/3.71 no se tocaron), huérfanas (`order_id is None`) explícitamente exentas de duplicación entre sí; 21 tests nuevos (`TestDuplicateObservedIdentities`, `TestObservationWindowPropagation`), suite 5898→5919, 12/12 mutaciones N1-N12 detectadas, M28 reinyectada y detectada, todas las garantías previas de 3.77 re-sondeadas y confirmadas intactas; **Hito 3.77 corregido — pendiente de reauditoría adversarial final**; tras su aceptación siguen explícitamente sin decidir: drift-tolerance policy, stale-round rejection, identidad de cuenta simétrica, y si el motor completo será sólo-detección o incluirá reparación)

---

## Componente en curso: `execution_gateway` (Fase 3)

**Hitos completados:**
- 3.1 — Paquete `execution_gateway` inicializado (`__version__ = "0.1.0"`)
- 3.2 — `GatewayConfig` (`environment`, `dry_run`, `timeout_seconds`), 15 tests
- 3.3 — `ExecutionRequest` y `ExecutionResult` (`contracts.py`), 37 tests
- 3.4 — `ExecutionGateway` Protocol (`gateway.py`), 11 tests
- 3.5 — `FakeExecutionGateway` (`fake_gateway.py`), 15 tests
- 3.6 — `DryRunExecutionGateway` (`dry_run_gateway.py`), 18 tests
- 3.7 — `create_execution_gateway` factory (`factory.py`), 10 tests
- 3.8 — `GatewayConfig.environment` restringido a `"demo"` (D-011), 21 tests en config
- 3.9 — `BybitDemoCredentials` (`credentials.py`), 28 tests
- 3.10 — `BybitDemoClient` Protocol (`bybit_client.py`), 14 tests
- 3.11 — `BybitExecutionGateway` adaptador (`bybit_gateway.py`), 19 tests
- 3.12 — Factory ampliada: `dry_run=False` + `BybitDemoClient` → `BybitExecutionGateway`
- 3.13 — `HttpTransport` Protocol (`http_transport.py`), 17 tests
- 3.14 — `JsonSerializer` Protocol (`json_serializer.py`), 16 tests
- 3.15 — `StandardJsonSerializer` (`standard_json_serializer.py`), 32 tests
- 3.16 — `MillisecondClock` Protocol (`millisecond_clock.py`), 16 tests
- 3.17 — `SystemMillisecondClock` (`system_millisecond_clock.py`), 20 tests
- 3.18 — `MessageSigner` Protocol (`message_signer.py`), 21 tests
- 3.19 — `HmacSha256Signer` (`hmac_sha256_signer.py`), 22 tests
- 3.20 — `BybitAuthentication` + `BybitAuthenticator` (`bybit_authenticator.py`), 38 tests
- 3.21 — `StandardBybitAuthenticator` (`standard_bybit_authenticator.py`), 40 tests
- 3.22 — `BybitHeaderBuilder` (`bybit_header_builder.py`), 31 tests
- 3.23 — `UrllibHttpTransport` (`urllib_http_transport.py`), 45 tests
- 3.24 — `HttpRequest` + `BybitRequestBuilder` (`http_request.py`, `bybit_request_builder.py`), 58 tests
- 3.25 — `HttpRequestExecutor` (`http_request_executor.py`), 47 tests
- 3.26 — `BybitPrivateRequestSender` (`bybit_private_request_sender.py`), 58 tests
- 3.27 — `BybitResponse` (`bybit_response.py`), 59 tests
- 3.28 — `BybitResponseParser` (`bybit_response_parser.py`), 63 tests
- 3.29 — `BybitPrivateApi` (`bybit_private_api.py`), 52 tests
- 3.30 — `BybitEndpoint` (`bybit_endpoint.py`), 58 tests
- 3.31 — `BybitUrlBuilder` (`bybit_url_builder.py`), 52 tests
- 3.32 — `BybitEndpointExecutor` (`bybit_endpoint_executor.py`), 67 tests
- 3.33 — `BYBIT_CREATE_ORDER_ENDPOINT` (`bybit_endpoints.py`), 40 tests — endpoint declarado, operación todavía no implementada
- 3.34 — `BybitCreateOrderRequest` (`bybit_create_order_request.py`), 119 tests
- 3.35 — `BybitCreateOrderPayloadBuilder` (`bybit_create_order_payload_builder.py`), 73 tests
- 3.36 — `BybitCreateOrderOperation` (`bybit_create_order_operation.py`), 71 tests
- 3.37 — `BybitDemoClient.create_order` (`bybit_client.py`), 59 tests — fachada sobre `BybitCreateOrderOperation`; respuesta no interpretada
- 3.38 — `BybitCreateOrderResult` (`bybit_create_order_result.py`), 62 tests — value object de salida (`order_id`, `order_link_id`)
- 3.39 — `BybitCreateOrderResponseInterpreter` (`bybit_create_order_response_interpreter.py`), 73 tests — interpreta `BybitResponse` (ret_code==0) → `BybitCreateOrderResult`; no integrado en operación ni en cliente; `BybitDemoClient.create_order()` continúa devolviendo `BybitResponse`; `ret_code != 0` lanza `ValueError` temporalmente
- 3.40 — Integración del intérprete en `BybitCreateOrderOperation` (`bybit_create_order_operation.py`), 76 tests — cadena completa: `BybitCreateOrderRequest → BybitCreateOrderPayloadBuilder → BYBIT_CREATE_ORDER_ENDPOINT → BybitEndpointExecutor → BybitResponse → BybitCreateOrderResponseInterpreter → BybitCreateOrderResult`; `BybitDemoClient` no modificado
- 3.41 — Adaptación de `BybitDemoClient.create_order` al resultado interpretado (`bybit_client.py`), 64 tests — anotación de retorno cambiada de `BybitResponse` a `BybitCreateOrderResult`; import de `BybitResponse` eliminado; cadena pública completa: `BybitCreateOrderRequest → BybitDemoClient.create_order() → BybitCreateOrderOperation.execute() → BybitCreateOrderResult`; respuestas rechazadas (`ret_code != 0`) generan `ValueError` propagado desde el intérprete a través de la operación; cancelación, consultas y posiciones todavía no implementadas
- 3.42 — Excepción mínima para errores de la API de Bybit (`bybit_api_error.py`), 61 tests — `BybitApiError(Exception)` con campos `ret_code: int` y `ret_msg: str`; keyword-only; mensaje determinista `"Bybit API error {ret_code}: {ret_msg}"`; clasificación de errores y retries pendientes
- 3.43 — Integración de `BybitApiError` en `BybitCreateOrderResponseInterpreter` (`bybit_create_order_response_interpreter.py`), 80 tests — respuestas rechazadas (`ret_code != 0`) ahora lanzan `BybitApiError(ret_code=..., ret_msg=...)` en lugar de `ValueError`; `ret_code` y `ret_msg` conservados en el error; propagación intacta a través de `BybitCreateOrderOperation` y `BybitDemoClient`; clasificación de errores y retries pendientes
- 3.44 — Composition root del flujo de creación de órdenes de Bybit Demo (`bybit_demo_client_factory.py`), 54 tests — `create_bybit_demo_client(*, endpoint_executor: BybitEndpointExecutor) -> BybitDemoClient`; ensambla `BybitCreateOrderPayloadBuilder`, `BybitCreateOrderResponseInterpreter`, `BybitCreateOrderOperation` y `BybitDemoClient`; recibe un `BybitEndpointExecutor` ya construido; no construye transporte, sender ni credenciales; el cliente queda listo para creación de órdenes; respuestas rechazadas generan `BybitApiError`; cancelación, consultas y posiciones todavía no implementadas; configuración desde entorno pendiente
- 3.45 — Prueba integrada de creación de orden desde el gateway público (`test_execution_gateway_create_order_integration.py`), 94 tests — flujo de punta a punta validado en dos secciones: (A) desde `BybitDemoClient.create_order()` con `SpyExecutor`; (B) desde `BybitExecutionGateway.execute()` con `SpyPrivateApi` en la frontera inferior (`BybitPrivateApi`), `BybitEndpointExecutor` y `BybitUrlBuilder` reales; fix de producción mínimo: `place_order()` agregado a `BybitDemoClient` para conectar `BybitExecutionGateway.execute()` → `create_order()` (el `__subclasshook__` ya anticipaba este método); gateway público concreto: `BybitExecutionGateway`; método público probado: `execute()`; frontera simulada en la sección B: `SpyPrivateApi`; recorrido probado desde el gateway, no sólo desde el cliente; no se realizó conexión real con Bybit Demo; transporte, configuración y credenciales productivas continúan pendientes; cancelación, consultas y posiciones no implementadas
- 3.46 — Composition root del gateway público de Bybit Demo (`bybit_demo_execution_gateway_factory.py`), 63 tests — `create_bybit_demo_execution_gateway(*, private_api: BybitPrivateApi) -> BybitExecutionGateway`; ensambla internamente `BybitUrlBuilder` (URL hardcoded `https://api-demo.bybit.com`), `BybitEndpointExecutor` y delega en `create_bybit_demo_client`; recibe únicamente `private_api` ya construido; no lee entorno, no construye transporte ni credenciales; exportado en `__all__`; cobertura completa: API pública, validación, grafo de dependencias, reutilización de factory, múltiples llamadas, ausencia de ejecución en construcción, flujo integrado éxito+rechazo, ausencia de responsabilidades adicionales
- 3.47 — Composition root de `BybitPrivateApi` con dependencias inyectadas (`bybit_private_api_factory.py`), 65 tests — `create_bybit_private_api(*, sender: BybitPrivateRequestSender, response_parser: BybitResponseParser) -> BybitPrivateApi`; recibe `sender` (I/O, transporte, auth, firma) y `response_parser` (puro, contiene `JsonSerializer`) ya construidos; no lee variables de entorno; no recibe API key ni API secret como strings; no construye transporte, sesiones ni credenciales; no firma ni genera headers durante construcción; conserva ambas dependencias por identidad; cada llamada devuelve instancia nueva; validado compositivamente con `create_bybit_demo_execution_gateway`; todavía no existe composition root desde configuración externa; todavía no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.48 — Composition root de `BybitResponseParser` (`bybit_response_parser_factory.py`), 66 tests — `create_bybit_response_parser(*, serializer: JsonSerializer) -> BybitResponseParser`; recibe `serializer` (Protocol `@runtime_checkable`, acepta compatibilidad estructural) ya construido; conserva dependencia por identidad; no ejecuta parsing durante construcción; no llama a `dumps` ni `loads` al construir; no lee variables de entorno; no construye transporte ni sender; composición del sender privado sigue pendiente; todavía no existe composition root desde configuración externa; todavía no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.49 — Composition root de `BybitPrivateRequestSender` (`bybit_private_request_sender_factory.py`), 78 tests — `create_bybit_private_request_sender(*, request_builder: BybitRequestBuilder, request_executor: HttpRequestExecutor) -> BybitPrivateRequestSender`; `request_builder` contiene autenticador (secretos, firma) y serializer; `request_executor` contiene transporte (I/O); ambos inyectados ya construidos; conserva ambas dependencias por identidad; no llama al builder ni executor durante construcción; sin retries; error de transporte propagado por identidad sin wrapping; validado compositivamente hasta gateway; composition root de `BybitRequestBuilder` y `HttpRequestExecutor` sigue pendiente; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.50 — Composition root de `BybitRequestBuilder` (`bybit_request_builder_factory.py`), 88 tests — `create_bybit_request_builder(*, serializer: JsonSerializer, authenticator: BybitAuthenticator, header_builder: BybitHeaderBuilder) -> BybitRequestBuilder`; `serializer` (Protocol estructural, serializa payload); `authenticator` (Protocol estructural, contiene secretos, firma, reloj); `header_builder` (clase concreta, nominal, pura — inyectada para mantener visibilidad del grafo); conserva las tres dependencias por identidad; no serializa, no autentica, no firma, no consulta reloj durante construcción; error de autenticación propagado por identidad sin retry; validado compositivamente hasta gateway; composition root de `HttpRequestExecutor` y `StandardBybitAuthenticator` sigue pendiente; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.51 — Composition root de `StandardBybitAuthenticator` (`bybit_authenticator_factory.py`), 97 tests — `create_bybit_authenticator(*, credentials: BybitDemoCredentials, clock: MillisecondClock, signer: MessageSigner, recv_window_ms: int) -> StandardBybitAuthenticator`; `credentials` nominal (`BybitDemoCredentials`); `clock` (Protocol estructural, implementación mínima: `now_ms()`); `signer` (Protocol estructural, implementación mínima: `sign(*, secret, message)`); `recv_window_ms` int > 0, bool rechazado; no llama a clock ni signer durante construcción; error de clock y signer propagado por identidad; satisface `BybitAuthenticator` Protocol; validado compositivamente con `create_bybit_request_builder` hasta gateway; composition root de `HttpRequestExecutor` sigue pendiente; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.52 — Composition root de `HttpRequestExecutor` (`http_request_executor_factory.py`), 85 tests — `create_http_request_executor(*, transport: HttpTransport, timeout_seconds: float) -> HttpRequestExecutor`; `transport` (Protocol estructural `@runtime_checkable`, acepta cualquier objeto con `post(*, url, headers, body, timeout_seconds)`); `timeout_seconds` int o float > 0, bool rechazado; conserva `transport` por identidad; conserva `timeout_seconds` exactamente; no llama al transporte durante construcción; no abre conexiones ni crea sesiones; errores del transporte propagados por identidad sin wrapping ni retry; validado compositivamente hasta gateway completo; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.53 — Composition root del transporte HTTP productivo (`http_transport_factory.py`), 62 tests — `create_http_transport() -> UrllibHttpTransport`; sin parámetros: `UrllibHttpTransport.__init__` no recibe dependencias (usa `urllib.request.urlopen` directamente en `post()`); implementación concreta única que satisface `HttpTransport`; no llama a `urlopen` durante construcción; no abre conexiones ni resuelve DNS; no crea cliente ni sesión; errores de red propagados sin retry; validado compositivamente hasta gateway completo; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.54 — Composition root del serializer JSON productivo (`json_serializer_factory.py`), 76 tests — `create_json_serializer() -> StandardJsonSerializer`; sin parámetros: `StandardJsonSerializer.__init__` no recibe dependencias; delega `dumps` a `json.dumps` y `loads` a `json.loads` sin opciones especiales; satisface `JsonSerializer` Protocol; cada llamada crea instancia nueva (sin singleton); puede compartirse por identidad entre `create_bybit_request_builder` y `create_bybit_response_parser`; no llama a `dumps` ni `loads` durante construcción; no realiza I/O; no lee variables de entorno; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.55 — Composition root de `BybitHeaderBuilder` (`bybit_header_builder_factory.py`), 64 tests — `create_bybit_header_builder() -> BybitHeaderBuilder`; sin parámetros: `BybitHeaderBuilder.__init__` no recibe dependencias; `build(*, authentication: BybitAuthentication)` produce 5 headers (`X-BAPI-API-KEY`, `X-BAPI-TIMESTAMP`, `X-BAPI-RECV-WINDOW`, `X-BAPI-SIGN`, `Content-Type: application/json`); validación nominal de `BybitAuthentication`; cada llamada crea instancia nueva; no construye headers durante construcción; no realiza I/O; no lee variables de entorno; validado compositivamente con `create_bybit_request_builder` hasta gateway completo; no existe composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.56 — Composition root de `BybitDemoCredentials` desde secretos explícitos (`bybit_demo_credentials_factory.py`), 107 tests — `create_bybit_demo_credentials(*, api_key: str, api_secret: str) -> BybitDemoCredentials`; delega directamente al constructor del dataclass `frozen=True`; `api_key`: str no vacío ni whitespace-only → `ValueError`; tipo no-str → `TypeError` con mensaje `"api_key must be str, got: ..."`, bool rechazado como no-str; `api_secret`: misma validación con mensaje `"api_secret must not be empty or whitespace-only"`; `repr` actual: oculta secreto (`api_secret = field(repr=False)`) — solo muestra `api_key`; factory no lee variables de entorno; no registra ni imprime secretos; no valida credenciales contra Bybit; no crea authenticator, signer ni clock; no ejecuta red; cada llamada crea instancia nueva (sin singleton ni caché); valores no transformados (no strip, no normalización); integrada con `create_bybit_authenticator` y con stack completo hasta gateway; pendiente: composition root desde configuración externa (Railway vars / `.env`); no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.57 — Composition root del `MessageSigner` productivo (`message_signer_factory.py`), 85 tests — `create_message_signer() -> HmacSha256Signer`; sin parámetros; implementación concreta: `HmacSha256Signer`; satisface `MessageSigner` Protocol; algoritmo: HMAC-SHA256; biblioteca: `hmac` + `hashlib` (stdlib); encoding: UTF-8; digest: SHA-256; formato de salida: hexadecimal lowercase, 64 caracteres; acepta secret y message vacíos; no firma durante construcción; no recibe secretos; no lee variables de entorno; conservado por identidad en `StandardBybitAuthenticator._signer`; cada llamada crea instancia nueva; integrado hasta gateway completo; pendiente: composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.58 — Composition root del `MillisecondClock` productivo (`millisecond_clock_factory.py`), 86 tests — `create_millisecond_clock() -> SystemMillisecondClock`; sin parámetros; implementación: `SystemMillisecondClock`; fuente: `time.time_ns()` (ns Unix epoch); unidad: ms; tipo: `int`; truncamiento `// 1_000_000`; no consulta reloj durante construcción; conservado por identidad en `StandardBybitAuthenticator._clock`; cada llamada crea instancia nueva; integrado hasta gateway completo; pendiente: composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.59 — Composition root del recv window de Bybit desde valor explícito (`bybit_recv_window_factory.py`), 90 tests — `create_bybit_recv_window_ms(*, recv_window_ms: int) -> int`; tipo: `int`; `bool` → `TypeError("recv_window_ms must be int, got: bool")`; no-int → `TypeError`; `<= 0` → `ValueError("recv_window_ms must be > 0, got: {v}")`; int > 0 → aceptado sin transformación; subclasses de `int` (no bool): aceptadas; sin límite superior; sin default; no convierte ni normaliza; se inyecta en `StandardBybitAuthenticator._recv_window_ms`; no lee variables de entorno; pendiente: composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.60 — Composition root del timeout HTTP desde valor explícito (`http_timeout_factory.py`), 94 tests — `create_http_timeout_seconds(*, timeout_seconds: int | float) -> int | float`; tipos aceptados: `int` y `float` (no bool); `bool` → `TypeError("timeout_seconds must be int or float, got: bool")`; no-int/no-float → `TypeError("timeout_seconds must be int or float, got: {type}")`; `<= 0` → `ValueError("timeout_seconds must be > 0, got: {v}")`; NaN (float): aceptado (nan ≤ 0 evalúa False en Python); +inf: aceptado; -inf: ValueError; Decimal/Fraction: TypeError; subclasses de `int` y `float` (no bool): aceptadas; sin límite superior; sin default; no convierte (int preservado como int, float como float); se inyecta en `HttpRequestExecutor._timeout_seconds`; no ejecuta HTTP durante composición; no lee variables de entorno; pendiente: composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes
- 3.61 — Composition root de la base URL de Bybit Demo desde valor explícito (`bybit_demo_base_url_factory.py`), 82 tests — `create_bybit_demo_base_url(*, base_url: str) -> str`; tipo: `str` puro; no-str → `TypeError("base_url must be str, got: {type}")`; vacío o solo whitespace → `ValueError("base_url must not be empty or whitespace-only")`; sin `https://` → `ValueError("base_url must start with 'https://', got: ...")`; host vacío (e.g. `"https://"`) → `ValueError("base_url must have a non-empty host, got: ...")`; path presente (incl. trailing slash) → `ValueError("base_url must not contain a path, got: ...")`; query string presente → `ValueError("base_url must not contain a query string, got: ...")`; fragment presente → `ValueError("base_url must not contain a fragment, got: ...")`; subclasses de `str`: aceptadas; valor retornado sin transformación; usa `urlparse` de stdlib; validación idéntica a `BybitUrlBuilder.__init__` pero sin crear el objeto; consumidor principal: `BybitUrlBuilder(base_url=...)` → concatena `self._base_url + endpoint.path`; URL canónica de producción: `"https://api-demo.bybit.com"` (constante privada en `bybit_demo_execution_gateway_factory.py`); factory no hardcodea ninguna URL; no lee variables de entorno; no crea transporte, autenticador ni URL builder; no realiza DNS ni conexiones; integrado con stack completo hasta gateway; pendiente: composition root desde configuración externa; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes

- 3.62 — Composition root integral del Bybit Demo Execution Gateway (`configured_bybit_demo_execution_gateway_factory.py`), 74 tests — `create_configured_bybit_demo_execution_gateway(*, api_key: str, api_secret: str, recv_window_ms: int, timeout_seconds: int | float) -> BybitExecutionGateway`; único composition root de alto nivel que construye el grafo completo (credenciales → signer → clock → recv window → authenticator → serializer → header builder → request builder → response parser → transport → timeout → request executor → sender → private api → gateway) reutilizando exclusivamente las factories de los hitos 3.44–3.61, sin instanciar ninguna clase concreta directamente; **diagnóstico previo:** no existía factory integral equivalente — `create_bybit_demo_execution_gateway` (3.46) es la más cercana pero parte de un `private_api` ya construido, no de secretos crudos; el grafo del hito, tal como fue especificado, listaba `create_bybit_demo_base_url()` como paso intermedio, pero esa factory **no se invoca** en la implementación final: su único consumidor real es `BybitUrlBuilder`, y ese objeto ya se construye internamente dentro de `create_bybit_demo_execution_gateway` (3.46) a partir de la constante privada `_BYBIT_DEMO_BASE_URL`, sin aceptar `base_url` como parámetro; invocar `create_bybit_demo_base_url()` por separado habría producido un valor sin ningún destino en el grafo (llamada muerta) o habría exigido reimplementar la construcción de `BybitUrlBuilder`/`BybitEndpointExecutor`/`BybitDemoClient` en este hito, duplicando lógica ya resuelta en 3.46 — decisión: delegar el ensamblaje final directamente en `create_bybit_demo_execution_gateway(private_api=...)`, que ya reutiliza esa lógica sin modificarla; `create_bybit_demo_base_url` sigue disponible como composition root independiente para cualquier consumidor futuro que sí necesite inyectar una URL explícita; `recv_window_ms` y `timeout_seconds` se validan explícitamente vía `create_bybit_recv_window_ms` y `create_http_timeout_seconds` antes de inyectarse (validación redundante con la interna de `create_bybit_authenticator`/`create_http_request_executor` es preexistente de hitos anteriores, no introducida aquí); `serializer` se construye una sola vez y se comparte por identidad entre `request_builder` y `response_parser`; todos los parámetros externos son keyword-only y sin default (no se inventa ningún valor); cada llamada crea un grafo completamente nuevo (sin singleton, sin caché, sin estado global); no ejecuta HTTP, DNS, sockets, reloj, firma ni autenticación durante la composición; no lee variables de entorno ni archivos; próximo límite arquitectónico: composition root desde configuración externa (Railway vars) todavía no existe — este hito compone el grafo desde valores explícitos, no desde el entorno; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones pendientes

- 3.62-fix — Corrección post-auditoría del Hito 3.62 (`tests/test_execution_gateway_configured_bybit_demo_execution_gateway_factory.py`, sin cambios en `platform/`), 84 tests — auditoría independiente (Opus 5) sobre el commit `823e6b8` clasificó `CORREGIR HITO 3.62`; hallazgo importante: 9 de los 10 tests de `TestIdentity` usaban `assert x is not None` o `isinstance(...)`, que no detectan sustitución ni duplicación de dependencias — confirmado empíricamente mutando la composición (serializer no compartido entre `request_builder`/`response_parser`) y observando que 9 tests seguían pasando; corrección: `TestIdentity` reescrita para espiar cada factory inferior importada dentro del módulo (vía `monkeypatch.setattr(_module, nombre, spy)`), capturar el objeto real retornado, y comparar por `is` contra el objeto efectivamente hallado en el grafo construido (`_build_with_capture`); 15 identidades/valores verificados así: credentials, signer, clock, recv_window_ms (valor+tipo), authenticator, serializer (compartido entre builder y parser), header_builder, request_builder, response_parser, transport, timeout_seconds (valor+tipo), request_executor, sender, private_api, gateway; se agregó `TestIdentityMutationSensitivity` con sustitución real de signer y transport, y un contador de invocaciones de `create_json_serializer` (debe ser exactamente 1); re-ejecutar la mutación original de la auditoría contra la suite corregida confirma que `test_serializer_identity_shared_between_builder_and_parser` ahora falla como se espera, y que la suite vuelve a pasar en verde contra la composición real sin mutar; hallazgo menor H2 (test tautológico `hasattr(factory, "send_order")`) eliminado — la ausencia de envío de órdenes ya está cubierta conductualmente por `test_no_http_post_during_construction`; hallazgo menor H3 corregido: se agregaron spies que lanzan si `StandardJsonSerializer.dumps`, `StandardJsonSerializer.loads` o `BybitHeaderBuilder.build` son invocados durante la composición, confirmando conductualmente su ausencia (antes sólo se verificaba por inspección estática del código fuente); no corregidos en este commit (fuera de alcance, deuda documentada en la auditoría): doble validación de `recv_window_ms`/`timeout_seconds` (preexistente de 3.51/3.52) y ausencia de consumidor productivo de `create_bybit_demo_base_url` (deuda de diseño de 3.61); `configured_bybit_demo_execution_gateway_factory.py` no fue modificada — la composición productiva ya era correcta, sólo faltaba la prueba

- 3.63 — Configuración tipada e inmutable de Bybit Demo (`bybit_demo_execution_config.py`), 111 tests — `BybitDemoExecutionConfig` (`@dataclass(frozen=True)`) con exactamente cuatro campos: `api_key: str`, `api_secret: str = field(repr=False)`, `recv_window_ms: int`, `timeout_seconds: int | float`; encapsula únicamente los cuatro valores externos que hoy recibe `create_configured_bybit_demo_execution_gateway`, sin agregar `base_url` ni `environment`; validación en `__post_init__` **reutilizando** las composition roots existentes (`create_bybit_demo_credentials`, `create_bybit_recv_window_ms`, `create_http_timeout_seconds`) en lugar de reescribir los checks a mano — los valores retornados por esas factories se descartan (nunca reemplazan los campos: no hay transformación, no hay strip, no hay conversión); mensajes y tipos de excepción son **byte-idénticos** a las factories inferiores, verificado con tests parametrizados de paridad directa; NaN y +inf siguen aceptados en `timeout_seconds` (mismo contrato que 3.60, sin corregirlo aquí); `repr()`/`str()` ocultan `api_secret` (heredado de `field(repr=False)`, mismo patrón que `BybitDemoCredentials`), `api_key` permanece visible (mismo contrato que el value object existente); igualdad por valor (semántica normal de dataclass, sin identidad personalizada); sin `slots` (no es convención existente en el proyecto); superficie mínima: sin `from_env`, `from_dict`, `load`, `parse`, `to_dict`, `masked`, `validate` ni ningún método público más allá de los generados por `dataclass`; construir el objeto no lee entorno, no lee archivos, no consulta reloj, no firma, no serializa, no ejecuta HTTP/DNS, no construye el gateway, transporte ni authenticator (verificado con spies); **no se modificó** `create_configured_bybit_demo_execution_gateway` — todavía recibe los cuatro valores sueltos; la adaptación de la factory integral para aceptar este config queda para el Hito 3.64; todavía no existe carga desde variables de entorno ni Railway (Hito 3.65); todavía no existe bootstrap de aplicación (Hito 3.66) ni conexión real con Bybit Demo (Hito 3.67); cancelación, consultas y posiciones siguen pendientes; `execution_gateway` permanece abierto, no congelado

- 3.64 — Composition root integral adaptado para recibir `BybitDemoExecutionConfig` (`configured_bybit_demo_execution_gateway_factory.py`), 107 tests — firma nueva: `create_configured_bybit_demo_execution_gateway(*, config: BybitDemoExecutionConfig) -> BybitExecutionGateway`; **firma anterior eliminada por completo** (`api_key`, `api_secret`, `recv_window_ms`, `timeout_seconds` sueltos) — sin wrapper legacy, sin overload, sin deprecation warning; una llamada con la firma antigua falla naturalmente con `TypeError: unexpected keyword argument`; validación nominal explícita al inicio (`isinstance(config, BybitDemoExecutionConfig)`), rechaza `None`, `dict`, `tuple`, objetos arbitrarios y objetos estructurales con los mismos cuatro atributos (duck typing rechazado — la validación es nominal, no estructural); internamente la factory lee `config.api_key`, `config.api_secret`, `config.recv_window_ms`, `config.timeout_seconds` y los pasa **sin transformación** (verificado por identidad para los strings y por valor+tipo para los numéricos) a exactamente el mismo grafo de composición aceptado en el Hito 3.62 — ninguna factory inferior fue modificada, ninguna se agregó ni se quitó; se conservan las llamadas a `create_bybit_demo_credentials`, `create_bybit_recv_window_ms` y `create_http_timeout_seconds` como composition roots productivos aunque el config ya validó esos valores en su propio `__post_init__` (no se elimina la doble validación preexistente de 3.51/3.52/3.59/3.60, sigue fuera de alcance); el config no se muta, no se reemplaza, no se serializa (`dataclasses.asdict` nunca se invoca) y no se reconstruye dentro de la factory (verificado con spy sobre `BybitDemoExecutionConfig.__init__`); reutilizar el mismo objeto `config` en dos llamadas produce dos grafos completamente independientes; excepciones de factories inferiores sustituidas por spies (`create_bybit_demo_credentials`, `create_bybit_authenticator`, `create_http_request_executor`, `create_bybit_demo_execution_gateway`) propagan por identidad exacta, sin envolver; los 15 tests de identidad, los 3 de sensibilidad a mutación y todos los spies de ausencia de ejecución (reloj, firma, authenticate, dumps, loads, header build, HTTP, DNS, socket, entorno, archivos) del Hito 3.62 se conservaron intactos, sólo adaptados a construir el `config` antes de invocar la factory; correcciones menores aplicadas al Hito 3.63 (sin tocar producción del config): se agregaron 4 tests de orden de validación (`api_key` → `api_secret` → `recv_window_ms` → `timeout_seconds`, se detiene en el primero inválido) y se reemplazó la assertion tautológica de fuga de secreto por un marcador inequívoco (`ZZTOPSECRETMARKER9999`); siguiente límite arquitectónico: carga segura de `BybitDemoExecutionConfig` desde variables de entorno / Railway (Hito 3.65) — todavía no se leen variables de entorno en ningún punto de la cadena; todavía no existe bootstrap de aplicación (3.66) ni conexión real con Bybit Demo (3.67); cancelación, consultas y posiciones siguen pendientes; `execution_gateway` permanece abierto, no congelado

- 3.65 — Carga segura de `BybitDemoExecutionConfig` desde variables de entorno (`bybit_demo_execution_config_env_loader.py`, `environment_configuration_error.py`), 109 tests — `load_bybit_demo_execution_config_from_env(*, environ: Mapping[str, str] | None = None) -> BybitDemoExecutionConfig`; capa explícita y mínima: entorno externo → lectura de strings → conversión mínima de tipos → `BybitDemoExecutionConfig`; **no construye el gateway, no crea credentials/signer/clock/transport/authenticator, no se conecta con Bybit, no ejecuta HTTP** — el único efecto es construir el dataclass ya existente; cuatro variables públicas exactas, sin aliases: `PHOENIX_BYBIT_DEMO_API_KEY`, `PHOENIX_BYBIT_DEMO_API_SECRET`, `PHOENIX_BYBIT_RECV_WINDOW_MS`, `PHOENIX_HTTP_TIMEOUT_SECONDS`; `environ=None` usa `os.environ` (releído en cada llamada, sin caché); un mapping explícito se usa en exclusiva, nunca mezclado con `os.environ` — permite tests deterministas sin tocar el entorno real; conversión mínima: `recv_window_ms` con `int(raw)` (rechaza `"5000.0"`, texto no numérico), `timeout_seconds` con `float(raw)` (acepta `"10"` → `10.0` y `"7.5"`; `"nan"`/`"inf"`/`"-inf"` convierten con éxito pero son rechazados después por `BybitDemoExecutionConfig`/`create_http_timeout_seconds`, la única fuente de verdad — el loader no duplica esa validación); credenciales preservadas exactamente, sin `strip`, valor vacío/whitespace delegado al contrato del config; orden de lectura fijo y determinista — `PHOENIX_BYBIT_DEMO_API_KEY` → `PHOENIX_BYBIT_DEMO_API_SECRET` → `PHOENIX_BYBIT_RECV_WINDOW_MS` → `PHOENIX_HTTP_TIMEOUT_SECONDS` —, se detiene en la primera variable ausente o inconvertible; nueva excepción mínima `EnvironmentConfigurationError` (hereda directo de `Exception`, constructor keyword-only `message: str`, mismo patrón que `ExecutionInfrastructureError`/`BybitApiError`) — se usa exclusivamente para variable ausente (`"Missing required environment variable: {name}"`) o conversión estructural inválida (`"Invalid integer/numeric environment variable: {name}"`); nunca para rechazos que ya pertenecen al contrato del config (esos propagan como el `TypeError`/`ValueError` original del config, sin envolver); ningún mensaje de error incluye el valor leído, verificado con marcador `ZZSUPERSECRET9999`; sin `.env`, sin `python-dotenv` (diagnosticado: no hay dependencia ni uso previo en el repo), sin Railway SDK, sin lectura de archivos, sin logging, sin print, sin defaults ocultos, sin selección de entorno/exchange; 8 mutaciones verificadas y detectadas (loader ignora el mapping explícito y usa `os.environ`, secret filtrado en mensaje de error, `strip()` aplicado a credenciales, default oculto en recv window, default oculto en timeout, NaN de timeout aceptado silenciosamente, resultados cacheados entre llamadas, orden de lectura alterado); exportado desde `execution_gateway.__init__` (`load_bybit_demo_execution_config_from_env`, `EnvironmentConfigurationError`) y agregado a `__all__`; todavía no existe bootstrap de aplicación (3.66) ni conexión real con Bybit Demo (3.67); cancelación, consultas y posiciones siguen pendientes; `execution_gateway` permanece abierto, no congelado

- 3.66 — Bootstrap seguro del Bybit Demo Execution Gateway desde entorno (`bybit_demo_execution_gateway_env_bootstrap.py`), 96 tests — `bootstrap_bybit_demo_execution_gateway_from_env(*, environ: Mapping[str, str] | None = None) -> BybitExecutionGateway`; composition root de más alto nivel: une exclusivamente las dos piezas ya aceptadas, `load_bybit_demo_execution_config_from_env` (3.65) → `create_configured_bybit_demo_execution_gateway` (3.64), sin llamar a ninguna factory inferior ni instanciar ninguna clase concreta directamente — el cuerpo completo son las dos líneas documentadas: `config = load_bybit_demo_execution_config_from_env(environ=environ)` seguido de `return create_configured_bybit_demo_execution_gateway(config=config)`; `environ` se delega sin modificación — `None` se pasa como `None`, un mapping explícito se pasa por identidad (verificado con spy: el loader recibe el mismo objeto, nunca una copia ni un `dict()`), sin mezclar con `os.environ`, sin mutarlo, sin conservar referencia tras retornar; el bootstrap no lee `os.environ` en ningún punto — sigue siendo responsabilidad exclusiva del loader (verificado por inspección de fuente: sin `os.environ`, sin `os.getenv`, sin `import os`); orden fijo loader → composition root, verificado con spies (una sola llamada a cada uno, en ese orden); si el loader falla, el composition root nunca se invoca; si el composition root falla, no se recarga el config ni se reintenta; ambos errores propagan por identidad exacta, sin envolver (`__cause__` no se agrega); el retorno es el objeto exacto producido por el composition root (verificado `is`), sin wrapper, proxy ni subclase; cada llamada produce un grafo completamente nuevo — dos mappings distintos producen dos gateways con credentials, authenticator, transport, sender, private API, operation y client todos distintos por identidad, con los valores correctos de cada config aplicados a cada grafo; sin singleton, caché, memoization ni registry — ninguna variable global nueva; construir el gateway mediante el bootstrap no ejecuta red, DNS, reloj, firma, autenticación, serialización, construcción de headers, transporte ni creación de orden (verificado con spies que fallan si se invocan); ningún secreto aparece en `repr`/`str` del gateway ni en ningún mensaje de error del bootstrap, verificado con marcador `ZZSUPERSECRETBOOTSTRAP9999` — el secreto sigue presente dentro de `credentials.api_secret`, como corresponde para poder autenticar; sin excepción propia nueva — los errores siguen perteneciendo íntegramente a las capas inferiores (`EnvironmentConfigurationError`, errores semánticos de `BybitDemoExecutionConfig`, errores del composition root); corrección menor asociada del Hito 3.65 (sólo tests, sin cambios en `platform/`): `test_environment_configuration_error_message_never_contains_marker` era vacuo — el merge de dicts `_env(...) | _without(...)` sobrescribía el marcador con el valor por defecto antes de provocar el error, por lo que la aserción de ausencia de fuga era trivialmente verdadera con cualquier comportamiento; corregido invirtiendo el orden del merge (`_without(...) | {marcador}`) y agregando una aserción explícita de que el marcador sobrevive en el mapping de entrada — confirmado que ahora detecta la mutación de fuga de secreto que antes pasaba inadvertida; 8 mutaciones verificadas y detectadas (bootstrap omite el loader y construye el config a mano, convierte el mapping a `dict`, usa `os.environ` directamente, llama al loader dos veces, llama al composition root dos veces, no pasa el config por identidad, cachea el gateway entre llamadas, ejecuta una operación local durante la construcción); exportado desde `execution_gateway.__init__` (`bootstrap_bybit_demo_execution_gateway_from_env`) y agregado a `__all__`, sin aliases; siguiente límite arquitectónico: un smoke test seguro de sólo lectura o diagnóstico de conectividad — todavía sin enviar órdenes salvo autorización explícita posterior; no se validó conexión real con Bybit Demo; cancelación, consultas y posiciones siguen pendientes; `execution_gateway` permanece abierto, no congelado

- 3.67 — Smoke test seguro de conectividad y autenticación Bybit Demo, exclusivamente de lectura (`smoke_test_result.py`, `bybit_demo_connectivity_smoke_test.py`), 88 tests — `smoke_test_bybit_demo_connection(*, environ: Mapping[str, str] | None = None) -> SmokeTestResult`; flujo `environ → bootstrap (3.66, que internamente usa el loader 3.65) → GET autenticado → SmokeTestResult`, sin construir manualmente el grafo — reutiliza los objetos ya construidos y validados por el bootstrap (`authenticator`, `header_builder`, `response_parser`, `base_url`, `timeout_seconds`), nunca vuelve a llamar a ninguna factory inferior; endpoint elegido: `GET /v5/user/query-api` ("Query API Key Information") — endpoint oficial de diagnóstico de Bybit V5, sin parámetros, que verifica en una sola llamada exactamente lo pedido (credenciales válidas, firma HMAC aceptada, timestamp y recv_window dentro de ventana, permisos de la API key) sin leer ni modificar órdenes, posiciones, balances ni configuración — introspecciona la propia API key que firma la solicitud, no ningún recurso de la cuenta; **no reutiliza `create_order` ni `execute()`, no envía ninguna orden dummy** — el `HttpTransport`/`UrllibHttpTransport` existentes están hardcodeados a `POST` (ningún componente de la cadena honra `BybitEndpoint.method`, deuda ya documentada en la Auditoría C), por lo que el propio transporte no puede emitir un GET real sin modificarse; dado que modificar `transport.py` estaba fuera de alcance, la llamada de red se implementa con un `urllib.request.Request(method="GET")` propio dentro del nuevo módulo, firmando con `authenticator.authenticate(body="")` (mensaje `ts+key+recv+""`, correcto para un GET sin query string) y construyendo los headers con el `BybitHeaderBuilder` real ya existente en el grafo — la firma HMAC y el parseo de la respuesta (`BybitResponseParser`, mismo envelope `retCode/retMsg/result/retExtInfo/time` en todos los endpoints V5) sí se reutilizan sin cambios; `ret_code != 0` se traduce a `BybitApiError` (excepción ya existente, sin jerarquía nueva); ninguna otra excepción se crea — errores de red, HTTP, timeout o `BybitResponseProcessingError` (JSON/UTF-8 malformado) propagan exactamente como los produce el stack existente; `SmokeTestResult` (dataclass `frozen=True`, campos `success: bool`, `endpoint: str`, `environment: str`, `server_time: int | None = None`, `account_type: str | None = None`) contiene únicamente datos seguros — `server_time` se toma del `time_ms` del envelope de respuesta (público en toda la API V5, no un secreto); `account_type` queda reservado sin poblar porque `/v5/user/query-api` no lo devuelve; nunca api_key, api_secret, firma, headers, body ni tokens, verificado con marcador `ZZSUPERSECRETSMOKE9999` ausente de `repr`/`str` del resultado y de todo mensaje de error; exactamente una llamada HTTP por invocación (verificada con spy sobre `urlopen`); sin caché, sin singleton, sin estado global — dos llamadas producen resultados y firmas distintas; prueba de integración con el pipeline productivo real (bootstrap real, authenticator real, `BybitHeaderBuilder` real, `urllib.request.Request` real) espiando únicamente `urlopen`, incluida una verificación de la firma HMAC recalculada de forma independiente contra los headers realmente enviados; 8 mutaciones verificadas y detectadas (GET→POST, reutiliza `create_order`, llama `execute()`, llama al endpoint dos veces, cachea el resultado, imprime el secreto, apunta a Mainnet, apunta al endpoint incorrecto); exportado desde `execution_gateway.__init__` (`smoke_test_bybit_demo_connection`, `SmokeTestResult`) y agregado a `__all__`; **no crea, no cancela ni modifica ninguna orden, posición, balance o configuración — sólo lectura**; no se implementó trading real; cancelación, consultas de posiciones y gestión de balances siguen fuera de alcance; `execution_gateway` permanece abierto, no congelado

- 3.68A — Runner one-shot seguro para el smoke test de Bybit Demo (`bybit_demo_smoke_runner.py`), 96 tests — módulo ejecutable de una sola corrida, pensado para Railway pero sin conocer Railway: comando `python3 -m execution_gateway.bybit_demo_smoke_runner` (con `platform/` en `PYTHONPATH`, la misma convención ya usada por pytest — `python -m platform.execution_gateway...` no es una ruta válida porque `platform/` no es un paquete Python, diagnosticado antes de implementar); `def main() -> int` invoca `smoke_test_bybit_demo_connection(environ=None)` **exactamente una vez** (sin retry, sin backoff, sin loop, sin segunda llamada en ningún camino, incluido `finally`), y el único `SystemExit` del módulo vive en el bloque `if __name__ == "__main__": raise SystemExit(main())` — `main()` en sí nunca llama `sys.exit`; no construye manualmente config/credentials/signer/authenticator/transport/gateway/parser — depende únicamente de la API pública `smoke_test_bybit_demo_connection`; salida saneada determinista: éxito imprime el encabezado `PHOENIX_BYBIT_DEMO_SMOKE_TEST_SUCCESS` seguido de `success=True`/`endpoint=`/`environment=`/`server_time=`/`account_type=` en ese orden exacto, con los opcionales ausentes renderizados como el literal `NONE` (no `None`); fallo imprime `PHOENIX_BYBIT_DEMO_SMOKE_TEST_FAILURE` + `error_type=`/`error_message=` únicamente — nunca traceback, nunca `repr(result)`, nunca `__cause__`; clasificación de errores sin crear jerarquía nueva: `EnvironmentConfigurationError` y `(TypeError, ValueError)` de validación de configuración usan `str(error)` (mensajes ya garantizados seguros por contrato desde los Hitos 3.63/3.65 — nunca contienen valores, sólo nombres de variable); `BybitApiError` usa el mensaje genérico fijo `"Bybit authentication or API validation failure"` (ni `ret_code` ni `ret_msg` se imprimen, decisión conservadora); `BybitResponseProcessingError` usa `str(error)` (mensaje constante, sin body remoto); `OSError` captura en una sola cláusula `HTTPError`/`URLError`/`TimeoutError` (las tres son subclases de `OSError` en la biblioteca estándar — verificado explícitamente antes de implementar, evita importar `urllib.error`/`socket` sólo para tipar excepciones) con el mensaje genérico `"Remote connectivity or HTTP failure"`; cualquier otra excepción (`RuntimeError`, `AttributeError`, `AssertionError`, etc.) usa el mensaje genérico fijo `"Unexpected smoke test failure"` — **nunca `str(error)`** para esta última categoría, para no arriesgar filtrar contenido no auditado; `KeyboardInterrupt`/`SystemExit` no se capturan (no heredan de `Exception`, por lo que un `except Exception` ya los deja pasar sin necesidad de manejo especial, verificado); `success=False` del propio resultado se trata como fallo (no se asume que sólo las excepciones representan error); exit codes exactamente `0`/`1`, sin códigos adicionales ni negativos; verificado con marcadores `ZZRUNNER*9999` que ningún secreto aparece en stdout/stderr en ninguna de las rutas de error; ejecución vía `runpy.run_module` confirma que `SystemExit.code` coincide con el valor de `main()`; importar el módulo no ejecuta nada (sin lectura de entorno, sin red, sin reloj, sin firma, sin print) — verificado con `importlib.reload` y spies que explotan si se invocan; 10 mutaciones verificadas y detectadas (llamada doble al smoke test, retorna 0 ante excepción, imprime el secreto, imprime traceback, imprime `repr(result)`, captura `KeyboardInterrupt`, ejecuta el smoke test al importar, llama `create_order`/`execute()`, trata `success=False` como éxito, error inesperado imprime `str(error)`) — la mutación de "ejecución durante el import" se verificó inyectando temporalmente la llamada a nivel de módulo en el archivo real (nunca commiteado) y confirmando que la sola colección de tests ya falla, restaurado byte a byte antes de continuar; el runner **no conoce Railway** — sin SDK, sin variables `RAILWAY_*`, sin `railway.json`/`railway.toml`, sin CLI — esa configuración queda para el Hito 3.68B; no exportado desde `execution_gateway.__init__` ni agregado a `__all__` (es un punto de entrada operativo, no API de la librería); todavía no se ejecutó ninguna conexión real con Bybit Demo; `execution_gateway` permanece abierto, no congelado

- 3.69 — Preparación del despliegue one-shot en Railway (`railway.toml`, corrección de un defecto real en `pyproject.toml`), 40 tests — diagnóstico obligatorio realizado en un venv temporal **fuera del repo** (creado y eliminado, nunca commiteado) antes de cambiar nada: `pip install .` contra el repo tal como estaba **fallaba siempre**, con `pip._vendor.pyproject_hooks._impl.BackendUnavailable: Cannot import 'setuptools.backends.legacy'` — `pyproject.toml` declaraba `build-backend = "setuptools.backends.legacy:build"`, un módulo que **no existe** en ninguna versión de `setuptools` (confirmado con `setuptools 83.0.0`: `setuptools.backends` no existe); el backend PEP 517 correcto de `setuptools` es `setuptools.build_meta`; este defecto habría bloqueado igual cualquier build estándar de Railway basado en `pip install`, no era específico de este entorno; corregido con un cambio de una sola línea, y **se pidió confirmación explícita al usuario antes de tocar `pyproject.toml`** (instrucción expresa del hito), aprobada antes de continuar; verificado el fix contra el repo real (no una copia) en un segundo venv temporal: `pip install .` construye el wheel y lo instala, `import execution_gateway` funciona **sin `PYTHONPATH`**, y el runner (`python3 -m execution_gateway.bybit_demo_smoke_runner`) se ejecutó dos veces sin red real y sin `PYTHONPATH` — sin credenciales → `exit 1` con la salida saneada esperada, con `urlopen` sustituido → `exit 0` con el formato de éxito exacto — ambas veces en un venv efímero, ya eliminado; `railway.toml` mínimo, limitado exactamente a build y start command — `buildCommand = "python3 -m pip install ."` (forma portable con `python3 -m`, preferida sobre `pip install .` a secas) y `startCommand = "python3 -m execution_gateway.bybit_demo_smoke_runner"` (el comando ya validado del Hito 3.68A, sin `python -c`, sin shell inline, sin servidor web); **deliberadamente sin** `restartPolicyType`, `healthcheckPath`, dominio, cron, réplicas, IDs de proyecto/servicio o cualquier variable `PHOENIX_*` — esa configuración es explícitamente manual en el dashboard de Railway, nunca asumida en el archivo; `tests/test_railway_smoke_service_config.py` parsea el TOML con `tomllib` (stdlib, sin dependencia nueva) y verifica build/start command exactos, ausencia de secretos/variables `PHOENIX_*`/Mainnet/Testnet/dominio/cron/healthcheck/IDs con forma de UUID/credenciales de GitHub, que el módulo del Start Command existe y es importable, y **dos tests de instalación real en venv temporal** (no simulados) que reproducen el escenario completo: instalar el paquete y ejecutar el runner sin `PYTHONPATH`; 10 mutaciones verificadas y detectadas sobre el contenido real de `railway.toml` (Start Command incorrecto, `python -c`, build que no instala el proyecto, dependencia de `PYTHONPATH`, servidor web, variable `PHOENIX_*`, secret, retry/loop, Mainnet, módulo inexistente) — cada mutación se escribió temporalmente sobre el archivo real y se restauró antes de continuar; **no se creó el servicio Railway, no se usó Railway CLI ni API, no se cargaron credenciales, no se ejecutó ninguna conexión real**; configuración manual pendiente en el dashboard de Railway: crear el servicio `phoenix-smoke-demo`, conectar el repo, desactivar autodeploy, `Restart Policy = Never`, sin dominio público, sin cron, cargar las cuatro variables `PHOENIX_BYBIT_DEMO_*`/`PHOENIX_HTTP_TIMEOUT_SECONDS` selladas manualmente, y disparar la corrida una sola vez; `execution_gateway` permanece abierto, no congelado

- Corrección operativa post-3.69 — Railway Restart Policy explícita (`railway.toml`), 47 tests — hallazgo de la auditoría del Hito 3.69: sin `restartPolicyType` declarado, Railway aplicaría su política por defecto `ON_FAILURE` (hasta 10 reintentos), y como el runner devuelve `exit 1` en **todo** camino de fallo — incluida la ausencia o mala carga de credenciales, el escenario más probable en la primera corrida —, eso habría producido hasta 10 solicitudes GET autenticadas contra Bybit Demo en vez de una sola, violando la garantía exactly-once construida en los Hitos 3.67/3.68A; agregado `restartPolicyType = "NEVER"` a `[deploy]` — única clave nueva, sin tocar `buildCommand` ni `startCommand`; `railway.toml` final: `[build] buildCommand = "python3 -m pip install ."` / `[deploy] startCommand = "python3 -m execution_gateway.bybit_demo_smoke_runner"` + `restartPolicyType = "NEVER"`; eliminadas las dos constantes muertas `_EXPECTED_BUILD_COMMAND`/`_EXPECTED_START_COMMAND` detectadas en la auditoría (nunca se usaban; las aserciones ya comparaban contra literales hardcodeados, así que eliminarlas no debilita ningún test); agregada la clase `TestRestartPolicy` — confirma presencia, valor exacto `"NEVER"`, rechazo explícito de `"ON_FAILURE"`/`"ALWAYS"`, sensibilidad al casing (`"never"`/`"Never"` rechazados por comparación exacta, no por normalización), ausencia de `restartPolicyMaxRetries`, ausencia de una segunda clave de política contradictoria, y que `[deploy]` sólo tiene `startCommand`+`restartPolicyType`; 8 mutaciones verificadas y detectadas sobre el archivo real (política ausente, `ON_FAILURE`, `ALWAYS`, casing incorrecto, `restartPolicyMaxRetries` agregado, clave de política contradictoria, build command alterado, start command alterado) — cada mutación escrita temporalmente y restaurada antes de continuar; **no se creó el servicio Railway, no se cargaron credenciales, no se ejecutó ninguna conexión real**; sigue pendiente la misma configuración manual documentada en el Hito 3.69 (crear `phoenix-smoke-demo`, conectar repo, autodeploy off, sin dominio, sin cron, variables selladas) — `restartPolicyType = "NEVER"` ya declarado en el archivo reduce ese riesgo específico, pero no reemplaza la revisión manual del resto de la configuración del servicio

- **Hito 3.68 — ACEPTADO** — validación real del smoke test ejecutado desde el servicio Railway `phoenix-smoke-demo`, configurado manualmente en el dashboard (guía paso a paso, sin Railway CLI/API): repo `garciarjosefina/phoenix-platform`@`main` conectado, sin dominio público, sin TCP proxy, sin cron, sin healthcheck, 1 réplica, `Restart Policy = Never` fijado explícitamente en la UI (defensa en profundidad sobre `railway.toml`), Config-as-code vinculado a `/railway.toml`, y las 4 variables `PHOENIX_BYBIT_DEMO_API_KEY`/`PHOENIX_BYBIT_DEMO_API_SECRET`/`PHOENIX_BYBIT_RECV_WINDOW_MS=5000`/`PHOENIX_HTTP_TIMEOUT_SECONDS=10` cargadas una por una (nunca por Raw Editor) y verificadas enmascaradas tras guardar. **Incidente 1 — `ModuleNotFoundError`:** primer deploy (región US West) construyó correctamente (`python3 -m pip install .` exitoso, wheel generado) pero el Start Command falló con `ModuleNotFoundError: No module named 'execution_gateway'`; auditoría de packaging aislada — extracción del commit exacto (`git archive HEAD`, sin cambios sin commitear) en un venv limpio fuera del repo, reproduciendo literalmente `python3 -m pip install .` — confirmó con evidencia (listado completo de las 79 entradas del wheel, `execution_gateway/` con sus 66 archivos incluido `bybit_demo_smoke_runner.py`, `python3 -m execution_gateway.bybit_demo_smoke_runner` ejecutado desde `/tmp` sin `PYTHONPATH`) que `pyproject.toml`/`setuptools`/`tool.setuptools.package-dir`/`tool.setuptools.packages.find` empaquetan e instalan correctamente el paquete — el defecto no está en el código versionado, sino en el ensamblado de imagen de Railpack (hipótesis no confirmada: una capa `copy /mise/installs...` posterior al `pip install .` en los Build Logs descarta el `site-packages` recién escrito); mitigado agregando `PYTHONPATH=/app/platform` como variable de Railway — **registrado como deuda técnica explícita, no solución definitiva** (`D-014` en `docs/decisions.md`): el `buildCommand` queda sin función real en el runtime, y `railway.toml` deja de ser la fuente completa de verdad del servicio (recrearlo desde el repo sin agregar `PYTHONPATH` manualmente reproduciría el mismo `ModuleNotFoundError`). **Incidente 2 — `HTTPError` por región:** con `PYTHONPATH` ya cargado, un segundo deploy (misma configuración, misma región US West) ejecutó el runner completo — cargó las 4 variables, construyó credenciales, firmó con HMAC — y llegó a intentar el GET real, pero devolvió `HTTPError` (el servidor respondió con un status de error; a diferencia de `URLError`, esto confirma que la conexión y el TLS sí se establecieron); diagnóstico por A/B limpio, mismo commit y misma configuración salvo una sola variable: cambiando únicamente la región a **EU West (Amsterdam, Netherlands / `europe-west4-drams3a`)** — la misma región donde ya corre `fib-shadow-canary` contra Bybit — el resultado fue `success=True`; registrado como regla de infraestructura oficial (`D-014`): todo servicio Railway conectado a Bybit se despliega por defecto en EU West, nunca en regiones de EE. UU. **Resultado final:** `PHOENIX_BYBIT_DEMO_SMOKE_TEST_SUCCESS` / `success=True` / `endpoint=/v5/user/query-api` / `environment=demo` / `server_time=1786129430735` / `account_type=NONE`; el `server_time` fue decodificado y verificado contra la hora real del log de despliegue (diferencia de 1 segundo), confirmando que el valor proviene genuinamente de la respuesta de Bybit y no puede haber sido generado por el propio código; deployment terminó `Completed`, sin `Crashed`, sin restart, sin retry, sin segunda ejecución (`restartPolicyType = "NEVER"` validado en producción en las 3 corridas de este hito, incluido el fallo por `ModuleNotFoundError`, que sólo generó 2 líneas de log en vez de 10 arranques); ningún log de ninguna de las 3 corridas contuvo api_key, api_secret, firma, headers, body remoto de Bybit ni traceback. **Frontera de validación explícita** — validado en producción real: variables de entorno, loader, `BybitDemoExecutionConfig`, bootstrap, composition root, credenciales, `HmacSha256Signer`, `BybitHeaderBuilder`, conectividad HTTPS, `GET /v5/user/query-api`, `recv_window` dentro de ventana, sincronía de reloj, API key Demo válida y con permisos, respuesta real de Bybit, `BybitResponseParser`, `SmokeTestResult`, el runner one-shot, `restartPolicyType = "NEVER"` — todavía **no** validado en producción, sólo por tests: `UrllibHttpTransport.post()` (el smoke test usa su propio `_get`, nunca ejercita el transporte productivo POST), `BybitCreateOrderOperation`/`BybitCreateOrderPayloadBuilder`/interpretación real de respuesta de orden, el flujo POST de trading completo, cancelación, consultas de posiciones y órdenes abiertas — ninguna orden real fue creada, cancelada ni consultada en ningún momento. Servicio `phoenix-smoke-demo` pendiente de eliminación manual (o desactivación de autodeploy) como acción operativa fuera de este repositorio, para evitar que un futuro push a `main` dispare una nueva ejecución mientras el servicio siga conectado; auditoría final realizada por un modelo distinto al que ejecutó el hito, siguiendo el mismo protocolo de auditorías anteriores; **sin cambios de código productivo, sin cambios en el runner/smoke test/authenticator/parser/transport/loader/bootstrap/factories** — este hito es exclusivamente de configuración de infraestructura, ejecución real y documentación; `execution_gateway` permanece abierto, no congelado

- **Hito 3.70 — Bybit Demo Positions Read** (`positions_contracts.py`, `positions_reader.py`, `bybit_positions_reader.py`, `bybit_positions_response_interpreter.py`, `bybit_private_get_api.py`, `bybit_private_get_request_sender.py`, `http_get_request_executor.py`, `http_get_transport.py`, `urllib_get_http_transport.py`, `bybit_demo_positions_reader_factory.py`, `configured_bybit_demo_positions_reader_factory.py`, `bybit_demo_positions_reader_env_bootstrap.py`, `bybit_demo_positions_query.py`), 321 tests nuevos — primera capacidad productiva de lectura de estado real de trading (base de la futura Reconciliation Engine/Portfolio Orchestrator/Risk Engine/Auditor, ninguno de los cuales se construyó en este hito). **Diagnóstico previo obligatorio:** la cadena POST existente (`BybitRequestBuilder→HttpRequestExecutor→UrllibHttpTransport`) está hardcodeada a POST (`UrllibHttpTransport.post` fija `method="POST"`) y `BybitEndpoint.method` es metadata declarada pero nunca consumida por `BybitEndpointExecutor`/`BybitUrlBuilder` (deuda ya documentada desde la Auditoría C/Hito 3.67) — generalizar esos componentes para soportar GET habría exigido tocar 3 clases con 45-90 tests cada una y usadas por todo el camino de creación de órdenes: descartado explícitamente como refactor amplio no autorizado. En su lugar se construyó una **primitiva GET nueva, paralela y reutilizable** (`HttpGetTransport` Protocol + `UrllibGetHttpTransport` + `HttpGetRequestExecutor` + `BybitPrivateGetRequestSender` + `BybitPrivateGetApi`, mismo split de responsabilidades 1:1 que su equivalente POST), que reutiliza sin duplicar `authenticator`/`header_builder`/`response_parser`/credenciales/`recv_window`/`timeout_seconds` ya existentes — mismo `BybitDemoExecutionConfig` y mismo loader de entorno que el Port de ejecución, **sin variables `PHOENIX_*` nuevas**; `BybitAuthenticator.authenticate(body=...)` ya era genérico y firma la query string de un GET exactamente igual que el body JSON de un POST (documentado en el Hito 3.67), reutilizado sin cambios. Endpoint oficial Bybit V5 `GET /v5/position/list`, agregado a `bybit_endpoints.py` (`BYBIT_POSITIONS_ENDPOINT`, archivo pre-existente extendido, no reescrito) — alcance deliberadamente fijado a `category=linear&settleCoin=USDT&limit=200` (Phoenix opera únicamente derivados lineares; `settleCoin` es obligatorio en la API V5 sin `symbol`; `limit=200` es el máximo de página de Bybit — **sin paginación en este hito, deuda documentada explícitamente, registrada en `ADR-002`**, no silenciosa: con el máximo de página ya solicitado sólo perdería datos con más de 200 posiciones simultáneas en una cuenta Demo). **Contratos de dominio nuevos** (`ExecutionPosition`, `PositionsSnapshot` en `positions_contracts.py`) — cero tipos/vocabulario Bybit: `symbol`/`side` (lowercase `"buy"`/`"sell"`, traducido desde `"Buy"`/`"Sell"` en la interpretación, igual que el mapeo inverso ya existente en `bybit_gateway.py`), `quantity`/`entry_price`/`leverage`/`unrealized_pnl` en `Decimal` con finitud validada (`.is_finite()`, rechaza NaN/±Infinity que Bybit sí puede construir desde esas strings literales); campos evaluados y descartados explícitamente por no estar justificados en este hito: `positionIdx` (hedge mode se preserva sin él — la combinación `(symbol, side)` ya distingue ambas piernas sin colapsarlas), `liquidation_price` (documentado como fuera de alcance, no silenciosamente omitido). Posiciones de tamaño 0 (placeholder de "sin posición", frecuente con `side="None"`) se **excluyen** del snapshot en la interpretación — no se modelan como una `ExecutionPosition` con `quantity=0` (el contrato exige `quantity > 0` como invariante propia, verificada independientemente de quién lo construya). **`PositionsReader` — Protocol nuevo, separado de `ExecutionGateway`** (decisión arquitectónica registrada formalmente en `docs/decisions.md` como `ADR-002`): ningún método de lectura se agregó a `ExecutionGateway`/`BybitDemoClient`; `BybitPositionsReader.query_positions()` traduce **todo** fallo (`BybitApiError` para cualquier `ret_code` — a diferencia del Port de escritura, una lectura no tiene noción de "rechazo de negocio" — `BybitResponseProcessingError`, `OSError`) a `ExecutionInfrastructureError` **ya existente**, mismo principio que ADR-001A, sin inventar jerarquía nueva; verificado con tests que ningún tipo `Bybit*` cruza `query_positions()` y que el mensaje de infraestructura nunca contiene el `ret_msg` original (sólo en `__cause__`). API pública `query_bybit_demo_positions(*, environ: Mapping[str, str] | None = None) -> PositionsSnapshot`, mismo patrón bootstrap-y-ejecutar en una función que `smoke_test_bybit_demo_connection` (Hito 3.67) — exactamente una llamada HTTP por invocación, sin caché, sin estado mutable a nivel de módulo, dos llamadas consecutivas producen resultados independientes. Cobertura de tests: contratos (inmutabilidad, tipos exactos, Decimal preservado sin conversión a float, rechazo de vocabulario Bybit capitalizado), respuesta vacía (`positions == ()`, no es error), una posición long, una posición short, múltiples posiciones (orden determinista, sin pérdida/duplicación), hedge mode (dos piernas del mismo symbol preservadas, no colapsadas), numéricos (cantidades pequeñas, muchos decimales, valores grandes, tamaño cero excluido, tamaño negativo rechazado, NaN/Infinity rechazados en las 4 magnitudes), respuesta malformada (result ausente, list ausente/no-list, item no-mapping, symbol/side/size/avgPrice/leverage/unrealisedPnl inválidos o ausentes, un ítem malformado entre válidos falla cerrado), exactly-once (una autenticación, una firma, una llamada HTTP, ningún reintento, verificado también con `restartPolicyType`-equivalente de "segunda llamada" explícitamente ausente), ausencia de trading (spies confirman cero referencias a `create_order`/`place_order`/`BybitCreateOrderOperation`/`/v5/order/create` en todo el read-side, verificado por inspección de fuente), pureza (ningún contrato de dominio importa nombres `Bybit*`), integración productiva real (bootstrap real, authenticator real, `BybitHeaderBuilder` real, firma HMAC recalculada independientemente contra la query string realmente enviada). **12/12 mutaciones verificadas y detectadas manualmente sobre el código real** (no simuladas): GET→POST, endpoint alterado, Demo→Mainnet, tipo Bybit crudo (`BybitApiError`) cruzando el Port sin traducir, `quantity` convertida a float (37 tests detectaron esta sola), side Buy/Sell invertidos, segunda llamada HTTP agregada, respuesta vacía tratada como error (24 tests), posiciones de hedge colapsadas por symbol, bypass de autenticación (headers vacíos), campos requeridos sin validar (el mutante ni corre limpio, explota con `KeyError` crudo), import accidental de `BybitCreateOrderRequest` en el módulo público — cada mutación aplicada sobre el archivo real, verificada como detectada, y restaurada; restauración final verificada byte-a-byte contra el original con `diff` antes de continuar. Suite completa re-ejecutada tras cada restauración: 4478 passing en el estado final, 0 regresiones. Sin conexión real con Bybit en ningún momento, sin ninguna orden creada/cancelada/modificada, sin tocar Railway ni `railway.toml`, `PYTHONPATH=/app/platform` (deuda del Hito 3.68) fuera de alcance, no tocado; `execution_gateway` permanece abierto, no congelado

- **Corrección post-3.70 — hallazgos IMPORTANTES de la auditoría independiente** (`positions_contracts.py`, `bybit_positions_response_interpreter.py`, tests de contratos/interpreter/reader), 35 tests nuevos — auditoría independiente (modelo distinto al que implementó el hito) clasificó `CORREGIR HITO 3.70` con 3 hallazgos IMPORTANTES y 1 documental. **IMPORTANT-1 (fail-closed ante paginación):** la auditoría demostró empíricamente que un `nextPageCursor` no vacío se ignoraba en silencio, devolviendo un `PositionsSnapshot` que aparentaba estar completo pero podía estar truncado — la peor falla posible para un futuro Reconciliation Engine (confundiría "sin más posiciones" con "hay más y no se pidieron"); corregido con una única línea en `interpret()`: cualquier `nextPageCursor` truthy (incluido whitespace-only) lanza `BybitResponseProcessingError` en vez de construir el snapshot; ausencia de la clave o cadena vacía siguen permitidas; no se implementa el follow-up de cursor (deuda ya documentada, ahora falla ruidosamente en vez de corromper en silencio). **IMPORTANT-2 (campos accesorios opcionales):** la auditoría demostró que `leverage=""` — respuesta real y válida de Bybit para cuentas Unified en portfolio margin — abortaba la consulta completa por una sola fila con un dato no esencial en blanco; `ExecutionPosition.leverage`/`unrealized_pnl` pasan a `Decimal | None = None` (mismo patrón que `ExecutionRequest.price` en `contracts.py`), el interpreter trata `""`/ausencia de clave como `None` sin abortar la fila, pero sigue exigiendo un `Decimal` finito válido si el valor está presente y no vacío — un accesorio malformado nunca se acepta en silencio; `symbol`/`side`/`quantity`/`entry_price` permanecen obligatorios sin ningún cambio; se evaluó explícitamente `avgPrice="0"` (pedido puntual de la auditoría, para no imponer una regla económica que Bybit no garantiza) y se decidió mantenerlo inválido — por construcción sólo se evalúa tras confirmar `quantity > 0`, y no existe ningún caso documentado de Bybit de una posición realmente abierta con precio promedio de entrada no positivo. **IMPORTANT-3 (garantía conductual de ausencia de caché):** la auditoría reprodujo la mutación exacta (`self._cached` inyectado en `BybitPositionsReader.query_positions()`) que había sobrevivido a la suite original — el hallazgo era de cobertura, no de comportamiento: no existía caché en producción, pero tampoco ningún test invocaba `query_positions()` dos veces sobre la misma instancia para poder detectarlo; se agregó `TestNoCacheAcrossCalls` (7 tests) que llama la misma instancia dos veces con respuestas distintas de un `_SpyPrivateGetApi`/`_SpyInterpreter` extendidos con soporte de cola de resultados (`results=[...]`), confirmando dos llamadas reales a la API, dos al interpreter, snapshots distintos por identidad, el segundo snapshot reflejando genuinamente la segunda respuesta (verificado de punta a punta con el `BybitPositionsResponseInterpreter` real, no un spy), y ausencia de cualquier atributo `_cached`/`_cache`/`_last_result`/`_last_snapshot` en la instancia; reinyectando la mutación original, la nueva suite la detecta (5/7 tests fallan). **IMPORTANT-4 (documental, sin cambio de código):** la auditoría confirmó que `(symbol, side)` preserva correctamente ambas piernas de una posición hedged sin colapsarlas y que omitir `positionIdx` fue la decisión correcta para *observar* el estado — pero señaló que el snapshot no contiene la identidad operativa completa que Bybit exige para *actuar*: cerrar/modificar una posición en hedge mode requiere `positionIdx`, ausente del contrato; registrado explícitamente en `ADR-002`: `PositionsSnapshot` es observacional, no operativo — la futura capa de actuación deberá incorporar el slot/leg remoto antes de poder cerrar o modificar una posición; no se agrega `positionIdx` en esta corrección por falta de consumidor real. **Hallazgos MENORES explícitamente no corregidos** (tamaño 0 con campos secundarios sin validar, cierre de `Response` sin test dedicado, validación de timeout duplicada en 6 archivos) — ninguno bloquea la base observacional. **8/8 mutaciones de esta corrección verificadas y detectadas manualmente sobre el código real** (cursor no vacío ignorado, snapshot parcial devuelto con cursor, `leverage`/`unrealisedPnl` vueltos obligatorios de nuevo, accesorio malformado aceptado en silencio, caché por instancia reinyectada — la misma mutación exacta que había sobrevivido —, regresión de colapso de hedge legs por symbol) — cada mutación restaurada y verificada byte-idéntica contra el original con `diff`. Suite completa re-ejecutada tras cada restauración: 4513 passing en el estado final, 0 regresiones. **No se tocó** el stack GET (`http_get_transport.py`, `urllib_get_http_transport.py`, `http_get_request_executor.py`, `bybit_private_get_request_sender.py`, `bybit_private_get_api.py`), ningún composition root, `authenticator`, `parser`, loader, bootstrap, `railway.toml`, `phoenix_core`, ni ningún componente del Port de escritura — la decisión de la cadena GET paralela (ADR-002, Decisión 2) permanece sin cambios, reafirmada explícitamente: revisar el contrato HTTP existente antes de duplicar otra cadena si aparece un tercer verbo. Sin conexión real con Bybit, sin ninguna orden creada/cancelada/modificada; `execution_gateway` permanece abierto, no congelado

- **Hito 3.71 — Bybit Demo Open Orders Read** (`open_orders_contracts.py`, `open_orders_reader.py`, `bybit_open_orders_reader.py`, `bybit_open_orders_response_interpreter.py`, `bybit_demo_open_orders_reader_factory.py`, `configured_bybit_demo_open_orders_reader_factory.py`, `bybit_demo_open_orders_reader_env_bootstrap.py`, `bybit_demo_open_orders_query.py`), 280 tests nuevos — segunda primitiva del read-side, complementa Positions Read (Hito 3.70) hacia el futuro Exchange State Snapshot/Reconciliation Engine/detección de órdenes huérfanas. Endpoint oficial Bybit V5 `GET /v5/order/realtime` — devuelve exclusivamente órdenes no terminales (New/PartiallyFilled/Untriggered), a diferencia de `/v5/order/history`, que no se usa; agregado a `bybit_endpoints.py` (`BYBIT_OPEN_ORDERS_ENDPOINT`, archivo pre-existente extendido) — el scope guard `test_no_query_orders_endpoint` (que anticipaba `BYBIT_ORDER_REALTIME_ENDPOINT`) se actualizó explícitamente, mismo patrón que con `BYBIT_POSITIONS_ENDPOINT` en el Hito 3.70. Alcance `category=linear&settleCoin=USDT&limit=50` — mismo criterio linear/USDT que Positions, `limit=50` es el máximo de página documentado por Bybit V5 para este endpoint específico (distinto del `limit=200` de `/v5/position/list`, no confirmado contra una respuesta real). **Reutiliza sin duplicar el GET stack completo del Hito 3.70** (`HttpGetTransport`/`UrllibGetHttpTransport`/`HttpGetRequestExecutor`/`BybitPrivateGetRequestSender`/`BybitPrivateGetApi`, `authenticator`/`header_builder`/`response_parser`/credenciales/`recv_window`/`timeout_seconds`, mismo `BybitDemoExecutionConfig` y loader de entorno — cero variables `PHOENIX_*` nuevas); Positions Read no fue tocado salvo exports aditivos en `__init__.py`; verificado por diff exacto contra el commit previo. **Identidad dual — punto crítico del hito, resuelto explícitamente**: `exchange_order_id: str` (orderId de Bybit, siempre presente y obligatorio para toda orden real) y `order_id: str | None` (dominio, == orderLinkId — mismo patrón ya establecido en `ExecutionResult`/`bybit_gateway.py` desde el ADR-001, pero invertido: aquí es el id de dominio el que puede faltar, no el de Bybit); `orderLinkId` ausente o `""` se traduce a `order_id=None` — nunca se inventa un id, nunca se oculta la orden del snapshot: una orden con `order_id=None` es precisamente la señal de una posible orden huérfana que el futuro reconciliador deberá poder detectar (verificado con tests dedicados: `test_orphan_order_not_dropped_from_snapshot`, `test_two_orphans_same_symbol_not_collapsed`). Contrato `ExecutionOpenOrder` (`open_orders_contracts.py`) — cero tipos Bybit: `side`/`order_type` lowercase (`"buy"`/`"sell"`, `"market"`/`"limit"`), `status` como enum reducido y **acotado al universo real de `/v5/order/realtime`** (`"new"`/`"partially_filled"`/`"untriggered"` — estados terminales como `"filled"`/`"cancelled"` se rechazan explícitamente por estar fuera de este scope de lectura, verificado con `test_status_rejects_terminal_state`); `quantity`/`filled_quantity`/`price` en `Decimal`; `filled_quantity` (cumExecQty) preserva precisión para detectar partial fills, con **`>= 0` como única invariante** — deliberadamente **no** se exige `filled_quantity <= quantity` porque no está confirmado que Bybit garantice esa relación en todo momento para este endpoint (misma lección que `avgPrice="0"` en la corrección post-3.70: no imponer una regla de negocio no verificada); `price` opcional (`Decimal | None`) porque Bybit puede reportar `""` para market orders sin precio preestablecido (no confirmado empíricamente, documentado como asunción); `reduce_only` validado como `bool` estricto, rechaza `"true"` string. Interpreter no filtra ningún ítem (a diferencia de Positions, que descarta tamaño 0) — el propio endpoint ya sólo devuelve órdenes abiertas, cada entrada de `list` es una `ExecutionOpenOrder` legítima; sin deduplicación por atributos económicos, verificado con `TestNoAccidentalDeduplication` (dos órdenes con mismo symbol/side/price pero distinto orderId permanecen separadas). **Fail-closed ante paginación incluido desde el diseño inicial, no como corrección posterior** — aplicación directa y proactiva de IMPORTANT-1 de la auditoría del Hito 3.70: `nextPageCursor` truthy (cualquier valor no vacío, incluido whitespace) → `BybitResponseProcessingError`, nunca snapshot parcial. **`TestNoCacheAcrossCalls` (7 tests) incluido desde el primer commit**, mismo patrón que IMPORTANT-3 — misma instancia, dos `query_open_orders()`, dos llamadas reales verificadas a la API y al interpreter, snapshots distintos por identidad, sin `_cached`/`_cache`/`_last_result`. `BybitOpenOrdersReader.query_open_orders()` traduce todo fallo (`BybitApiError` cualquier ret_code, `BybitResponseProcessingError`, `OSError`) a `ExecutionInfrastructureError` ya existente — ningún tipo Bybit cruza el Port, mismo principio que `BybitPositionsReader`/ADR-001A; extensión de `ADR-002` registrada (`OpenOrdersReader` como segundo Port hermano de `PositionsReader`, misma decisión aplicada, sin ADR redundante). API pública `query_bybit_demo_open_orders(*, environ=None) -> OpenOrdersSnapshot`, mismo patrón bootstrap-y-ejecutar que Positions/smoke test. **20/20 mutaciones especificadas verificadas manualmente sobre el código real, 19/20 detectadas**: GET→POST, endpoint alterado, Demo→Mainnet, orderId/orderLinkId intercambiados, orden huérfana descartada, quantity→float, filled_quantity ignorada, partial fill tratado como full, cursor ignorado, doble HTTP, auth bypass, caché por instancia, órdenes duplicadas colapsadas, import accidental de create_order, respuesta malformada aceptada, secreto filtrado, timeout ignorado, snapshot vacío tratado como error — todas detectadas; **1 mutación sobrevivió: cierre de `Response` sin context manager en el transport GET compartido** — mismo hallazgo MENOR ya conocido y explícitamente no bloqueante desde la auditoría del Hito 3.70 (no introducido por este hito, heredado del stack GET reutilizado). Cada mutación restaurada y verificada byte-idéntica contra el original con `diff`. Suite completa re-ejecutada tras cada restauración: 4793 passing en el estado final, 0 regresiones. Sin conexión real con Bybit en ningún momento, sin ninguna orden creada/cancelada/modificada, sin tocar Railway; `execution_gateway` permanece abierto, no congelado

- **Corrección post-3.71 — hallazgos IMPORTANTES de la auditoría independiente** (`open_orders_contracts.py`, `bybit_open_orders_response_interpreter.py`, tests de contrato/interpreter), 19 tests nuevos — auditoría independiente (modelo distinto al que implementó el hito) clasificó `CORREGIR HITO 3.71` con 2 hallazgos IMPORTANTES; todo lo demás (identidad dual, orphans, partial fills, `reduceOnly`, paginación, no-caché, exactly-once, firma/query, GET stack, pureza, write-side) fue auditado y aceptado sin cambios. **IMPORTANT-1 (`price="0"` aborta el snapshot):** la auditoría reprodujo contra el pipeline real que `orderType="Market", price="0"` producía `ExecutionInfrastructureError`, pese a ser una respuesta legítima real de Bybit para market orders (el propio código previo lo admitía como asunción no confirmada: *"se asume que Bybit representa 'sin precio preestablecido' como cadena vacía... no confirmado contra una respuesta real de un market order"*); corregido: nueva función `_to_optional_price_decimal` (reemplaza a la genérica `_to_optional_finite_decimal`, que sólo se usaba para `price` en este archivo) — `""` y `"0"` (comparado **por valor** tras parsear con `Decimal`, no por texto, por lo que `"0.0"`/`"0.00"` también se cubren) se tratan igual, ambos → `None`; cualquier otro valor sigue exigido a ser un `Decimal` finito válido, y el propio contrato (`price > 0` en `ExecutionOpenOrder.__post_init__`) sigue rechazando precios negativos sin cambios. **Deliberadamente no generalizado**: el tratamiento "cero == ausencia" es específico de `price` en este archivo — el comentario explícito advierte no confundirlo con otros campos opcionales donde 0 es un valor real (`unrealisedPnl` en Positions Read, Hito 3.70). **IMPORTANT-2 (`orderStatus="Triggered"` aborta el snapshot):** la auditoría concluyó que `Triggered` es el estado transitorio legítimo de una orden condicional en la transición `Untriggered → Triggered → New`, observable en una carrera de lectura real contra `/v5/order/realtime`, y que rechazarlo hace fallar la consulta completa por una condición de carrera normal, no por datos malformados; corregido agregando `"Triggered": "triggered"` a `_STATUS_FROM_BYBIT` y `"triggered"` a `_VALID_STATUSES` (universo ahora `new`/`partially_filled`/`untriggered`/`triggered`) — **no colapsado a `"new"`** (perdería la distinción semántica) y **sin agregar estados terminales** (`Filled`/`Cancelled`/`Rejected`/`Deactivated` siguen, correctamente, fuera de scope y rechazados, verificado sin cambios en `test_status_terminal_rejected`/`test_status_cancelled_rejected`). **Decisión explícita sobre `openOnly`:** no se tocó la query en esta corrección — cerrar IMPORTANT-2 en la capa de interpretación (aceptar el estado como válido) resolvió el hallazgo sin necesidad de cambiar la query firmada; la dependencia del comportamiento por defecto de Bybit para `/v5/order/realtime` (sin `openOnly` explícito) queda documentada como condición conocida y aceptada, no resuelta — cambiar la query habría sido una modificación no estrictamente necesaria para este fix. **Cobertura nueva:** `TestPriceSemantics` (10 tests: `""`/`"0"`/`"0.00"` → `None`, clave ausente → `None`, Limit válido → `Decimal`, negativo/malformado/NaN/Infinity → error, orden Market con `price="0"` entre órdenes válidas no aborta el snapshot) y `TestStatusSemantics` (8 tests: los 4 estados válidos mapean correctamente, `Triggered` explícitamente no colapsa a `new`, estado terminal ilegítimo y desconocido siguen rechazados, orden `Triggered` entre órdenes válidas no aborta el snapshot) en el archivo del interpreter; un test de aceptación de `"triggered"` en el archivo de contrato. **8/8 mutaciones de esta corrección verificadas y detectadas manualmente sobre el código real** (`price="0"` vuelto a abortar, `price=""` vuelto a abortar, `price="0"` convertido a `Decimal(0)` en vez de `None`, `Triggered` vuelto a rechazarse, `Triggered` colapsado incorrectamente a `new`, estado desconocido aceptado, `price="0"` abortando snapshot multi-orden, `Triggered` abortando snapshot multi-orden) — cada mutación restaurada y verificada byte-idéntica contra el original con `diff`. Suite completa re-ejecutada tras cada restauración: 4812 passing en el estado final, 0 regresiones. **No se tocó** identidad `orderId`/`orderLinkId`, comportamiento orphan, `filled_quantity`, partial fill, `reduceOnly`, paginación, no-caché, exactly-once, GET stack, endpoint, composition roots, Positions Read, write-side, `authenticator`, `parser`, transport, `railway.toml`, `phoenix_core` — la deuda de cierre de `Response` sin test (MENOR, Hito 3.70) permanece explícitamente sin tocar, sin convertirse en refactor. `OpenOrdersSnapshot` sigue siendo puramente observacional; **Hito 3.71 pendiente de reauditoría final** tras esta corrección. Sin conexión real con Bybit, sin ninguna orden creada/cancelada/modificada; `execution_gateway` permanece abierto, no congelado

- **Reauditoría post-corrección 3.71 — `ACEPTAR HITO 3.71`**, sin cambios de código — reauditoría corta y focalizada (modelo distinto, no confiando en el resumen del implementador), verificando exclusivamente que IMPORTANT-1 y IMPORTANT-2 quedaron cerrados sin tolerancia excesiva ni regresión, no repitiendo la auditoría completa original. **Price semantics:** 11 probes conductuales contra el pipeline real (`query_bybit_demo_open_orders` con `urlopen` monkeypatcheado) — `""`/`"0"`/`"0.0"`/`"0.00"` → `None`, ausente → `None`, valor válido → `Decimal` exacto, negativo/NaN/Infinity/no-string → error, Market `price="0"` junto a orden Limit válida → snapshot completo (2 órdenes) — todos OK. **Status semantics:** 4 mapeos correctos (New/PartiallyFilled/Untriggered/Triggered), sin colapso, terminales/desconocidos siguen rechazados, Triggered junto a orden válida → snapshot completo — todos OK. **`openOnly`:** confirmado sin cambios (`_QUERY_STRING` idéntico), sin evidencia nueva que amerite pedir modificación. **Regresiones:** ninguna — `git diff --stat` entre el estado pre-corrección y `HEAD` confirmó cero cambios en identidad, orphans, `filled_quantity`, `reduceOnly`, paginación, no-caché (`TestNoCacheAcrossCalls` intacto), GET stack, endpoint/query, error model, composition roots, Positions Read, write-side, `phoenix_core`, `railway.toml`. **Response-close:** confirmado MENOR, `with urllib.request.urlopen(...) as response:` intacto en producción. **8/8 mutaciones de reinyección detectadas** (`price="0"` vuelto a abortar, `price="0"`→`Decimal(0)`, `price=""` vuelto a abortar, `Triggered` vuelto a rechazarse, `Triggered`→`"new"`, estado desconocido aceptado, `price="0"`/`Triggered` abortando snapshot multi-orden), cada una restaurada y verificada byte-idéntica. Suite: 4812 passing sin alteración (baseline confirmado antes y después). Sin modificación de archivos, sin commit, sin push, sin conexión real con Bybit, sin proponer el siguiente hito — cumplido en su totalidad.

- **Hito 3.72 — Bybit Demo Wallet/Balance Read** (`wallet_balance_contracts.py`, `wallet_balance_reader.py`, `bybit_wallet_balance_reader.py`, `bybit_wallet_balance_response_interpreter.py`, `bybit_demo_wallet_balance_reader_factory.py`, `configured_bybit_demo_wallet_balance_reader_factory.py`, `bybit_demo_wallet_balance_reader_env_bootstrap.py`, `bybit_demo_wallet_balance_query.py`), 291 tests nuevos — tercera primitiva del read-side, junto a Positions Read (3.70) y Open Orders Read (3.71), sin construir todavía el Reconciliation Engine ni un agregado `ExchangeStateSnapshot` (las tres primitives permanecen independientes por diseño, a la espera de que el Reconciliation Engine determine cómo deben combinarse). **Diagnóstico previo contra documentación oficial de Bybit V5** (no asumido desde el prompt): endpoint `GET /v5/account/wallet-balance`; `accountType` — enum confirmado con sólo dos valores para este endpoint, `UNIFIED` (Unified Trading Account) y `FUND` (billetera de depósito/retiro, endpoint distinto) — `UNIFIED` es la única opción coherente con una cuenta que opera derivados lineares USDT, fijada como constante igual que `category=linear&settleCoin=USDT` en Positions/Open Orders; estructura de respuesta confirmada con el ejemplo oficial — `result.list` es una tupla de **un** objeto de cuenta por `accountType` consultado (no una colección paginada), cada objeto de cuenta trae anidada su propia lista `coin` de balances por moneda; **sin `nextPageCursor` en el esquema documentado de este endpoint** — a diferencia de Positions/Open Orders, no se inventó un cheque de paginación para un campo que no existe aquí (verificado con test dedicado que confirma la ausencia del cheque en el código, y que la presencia inesperada de esa clave no tiene efecto alguno); documentación de Demo Trading confirmó que el endpoint está disponible para cuentas demo sin diferencias de comportamiento documentadas. **Distinción balance vs. capital disponible — objetivo central del hito, no "leer cuánto dinero hay"**: se investigaron los conceptos financieros de Bybit (wallet balance, equity, available balance, margin balance, initial/maintenance margin, unrealized PnL) antes de diseñar el contrato; cinco totales de cuenta elegidos y justificados individualmente, no copiados en bloque — `total_wallet_balance` (lo que la cuenta posee, sin PnL no realizado), `total_equity` (lo que vale ahora mismo incluyendo PnL no realizado — la distinción "lo que cree que posee vs. lo que Bybit reconoce"), `total_available_balance` (lo que realmente puede usarse para abrir posición nueva, el concepto que un naive `balance = walletBalance` habría ocultado), `total_initial_margin`/`total_maintenance_margin` (margen ya comprometido y umbral de liquidación, esenciales para un futuro Risk Engine); **deliberadamente excluidos** `totalMarginBalance`, `totalPerpUPL`, `accountIMRate`/`accountMMRate` — derivables o redundantes con los cinco ya presentes para el alcance linear-USDT actual de Phoenix, no agregados "por si acaso". **Balance por moneda** (`ExecutionCurrencyBalance`, tupla `currency_balances` dentro del mismo `WalletBalanceSnapshot` — no un segundo Port, no un agregado nuevo: la propia respuesta de Bybit ya unifica totales+monedas en una sola llamada HTTP, separar en dos lecturas habría violado exactly-once sin ningún límite natural que lo justifique): `coin`/`wallet_balance`/`equity` esenciales; `unrealized_pnl`/`usd_value` accesorios, mismo patrón que `leverage`/`unrealized_pnl` en Positions Read; ninguna moneda se descarta silenciosamente (`test_unknown_currency_not_discarded`), no se asume que siempre habrá exactamente USDT — se investigó explícitamente si a nivel de moneda existe un concepto de "disponible" propio y se confirmó que no (`availableToWithdraw`/`availableToBorrow`/`free` son campos deprecated en el objeto `coin`; "disponible" es, correctamente, un concepto sólo de cuenta bajo margen cruzado/portfolio, no por moneda — hallazgo documentado, no inventado). **Ceros y vacíos clasificados explícitamente por campo antes de codificar** (lección de 3.70/3.71 aplicada proactivamente desde el primer commit, no descubierta por auditoría): `total_initial_margin`/`total_maintenance_margin` — `"0"` es un valor legítimo (cuenta flat, sin posiciones/órdenes abiertas), validado `>= 0` estricto porque una magnitud de margen no puede ser negativa; `wallet_balance`/`equity`/`total_equity`/`total_available_balance` — sin restricción de signo (no se asume que Bybit garantiza no-negatividad; un escenario de pérdidas o cercanía a liquidación podría, en teoría, reportar valores negativos, y no está documentado lo contrario); accesorios (`unrealized_pnl`/`usd_value`) — `""`/ausente → `None`, `"0"` preservado como cero real (nunca colapsado a `None`), cualquier otro valor no vacío sigue exigido a ser un `Decimal` finito válido. **Reutiliza sin duplicar** el GET stack completo de 3.70/3.71 (`HttpGetTransport`/`UrllibGetHttpTransport`/`HttpGetRequestExecutor`/`BybitPrivateGetRequestSender`/`BybitPrivateGetApi`) y `BybitDemoExecutionConfig` — **cero variables `PHOENIX_*` nuevas**, verificado con test dedicado. `BybitWalletBalanceReader.query_wallet_balance()` traduce todo fallo a `ExecutionInfrastructureError` ya existente, mismo principio que `BybitPositionsReader`/`BybitOpenOrdersReader`/ADR-001A; `TestNoCacheAcrossCalls` incluido desde el primer commit (mismo patrón, no como corrección posterior); query canonicalizada verificada con HMAC recalculado independientemente contra `accountType=UNIFIED` (`TestQueryCanonicalization`, usando el pipeline productivo real de autenticación, no un mock del signer). **Deuda MENOR heredada de 3.70 cerrada en este hito** (cierre de `Response` sin test dedicado en el transport GET compartido, señalada explícitamente en la auditoría de 3.70 y no bloqueante desde entonces): `TestResponseClose` (2 tests nuevos en `test_execution_gateway_urllib_get_http_transport.py`) prueba que `urllib.request.urlopen` se usa como context manager — sin cambiar producción, que ya era correcta; mutante "eliminar el `with`" verificado como detectado antes de continuar. **Pureza auditada por AST, no sólo strings** (`TestPurityByAst`): imports reales de `wallet_balance_contracts.py`/`wallet_balance_reader.py` confirmados 100% stdlib (`dataclasses`/`decimal`/`typing` + el propio contrato), sin Bybit/urllib/HTTP. **291 tests nuevos, suite 4812→5103 (+291 tests netos); 15/16 mutaciones detectadas manualmente sobre el código real** (GET→POST, endpoint alterado, Demo→Mainnet, doble llamada HTTP, caché por instancia, `Decimal`→`float`, campo esencial ausente aceptado, `""` esencial aceptado indebidamente, `""` accesorio abortando snapshot, `"0"` legítimo (`total_initial_margin`) abortando snapshot, moneda omitida silenciosamente, error remoto filtrando `ret_msg`, `except Exception` agregado, snapshot vacío tratado como error, response no cerrado) — cada mutación restaurada y verificada byte-idéntica contra el original con `diff`; **1 mutación no produjo el fallo esperado en el punto exacto mutado** (quitar el chequeo `is_finite()` del helper `_to_finite_decimal` del interpreter para NaN/Infinity) **pero fue absorbida por validación redundante de defensa en profundidad**: `WalletBalanceSnapshot`/`ExecutionCurrencyBalance` ya validan `.is_finite()` independientemente en su propio `__post_init__`, por lo que el comportamiento final (NaN/Infinity rechazados) se mantuvo correcto — documentado explícitamente como hallazgo MENOR de cobertura (el test de la capa interpreter no es la única línea de defensa para esos dos campos), no como una brecha de comportamiento observable. Sin conexión real con Bybit en ningún momento, sin ninguna orden creada/cancelada/modificada, sin tocar Railway, `phoenix_core` ni el write-side; `execution_gateway` permanece abierto, no congelado.

- **Corrección post-3.72 — hallazgos de la auditoría adversarial independiente** (`bybit_wallet_balance_response_interpreter.py`, `wallet_balance_contracts.py`, tests de contrato/interpreter), 30 tests nuevos — auditoría adversarial independiente (atacando específicamente la semántica financiera, no sólo el código) clasificó `CORREGIR HITO 3.72` con 2 hallazgos IMPORTANTES y 5 MENORES. **IMPORTANTE-1:** investigación adicional contra documentación oficial confirmó que Bybit sólo declara una nota genérica ("all account wide fields not applicable to isolated margin") aplicada por igual a los cinco totales de cuenta, sin especificar qué representación toma cada uno — clasificados los cinco en categoría C (documentación insuficiente para relajar selectivamente ninguno); **se mantienen esenciales/fail-closed sin cambio de comportamiento**, evitando repetir el patrón de leverage=""/price="0" sobre el input más crítico del futuro Risk Engine; clasificación y razonamiento documentados extensamente junto a `_ACCOUNT_REQUIRED_FIELDS`, protegidos por `TestAccountWideFieldClassification` (6 tests). **IMPORTANTE-2:** comentario de `total_available_balance` corregido — deja de leerse como "capital disponible para abrir posición" (lectura engañosa que podría inducir a sobredimensionar posiciones) y pasa a documentar que es una magnitud de cuenta en equivalente USD, dependiente de margin mode, neta de un Haircut no definido por Bybit y multi-colateral, que el futuro Risk Engine deberá interpretar junto con `currency_balances`/moneda de settlement/colateral/margin mode, nunca aislada; protegido por `TestTotalAvailableBalanceSemantics` (4 tests). **5 hallazgos MENORES cerrados:** ausencia de USDT confirmada como resultado remoto legítimo vía documentación oficial ("returns non-zero asset info"), nunca sintetizada (`TestUsdtAbsencePermitted`); cobertura de negativos en `unrealized_pnl`/`usd_value`/`wallet_balance`/`equity` (comportamiento ya correcto, sólo faltaban tests); no-deduplicación de monedas protegida (`TestNoAccidentalDeduplication`, mismo patrón que Open Orders 3.71); mutante de `is_finite()` del interpreter reconfirmado como backstop completo del contrato (11 caminos no-finitos probados end-to-end, ninguno observable) — sin cambio de código; comentario de `result.list==1` corregido de "documentado por Bybit" a "invariante conservadora de Phoenix". **Advertencia de no-atomicidad agregada a ADR-002:** Positions/Open Orders/Wallet Balance provienen de tres requests HTTP independientes con `server_time_ms` propios — no constituyen un snapshot atómico conjunto; un futuro `ExchangeStateSnapshot`/Reconciliation Engine deberá medir drift temporal, definir ventana máxima aceptable, y tratar el estado combinado como eventualmente consistente (registrado como requisito futuro, sin implementar). **Response-close confirmado genuinamente cerrado**, retirado de la lista de deudas pendientes. **13/14 mutaciones de esta corrección verificadas y detectadas** (los cinco account-wide a `None`, `unrealized_pnl`/`usd_value` negativos rechazados, deduplicación de monedas, USDT=0 sintético, `result.list` vacío/`>1` aceptado, caché por instancia, `server_time_ms` ignorado, cierre de `Response` eliminado, `ret_msg` filtrado) — cada una restaurada y verificada byte-idéntica; la mutación restante reproduce el hallazgo MENOR-4 ya conocido y aceptado (NaN a nivel del helper del interpreter, absorbido por el contrato). Suite completa re-ejecutada: 5125 passing en el estado final, 0 regresiones. **No se tocó** ninguna lógica de validación productiva — todos los cambios en archivos de producción son comentarios/documentación; Positions Read, Open Orders Read, GET stack, composition roots, `railway.toml`, `phoenix_core`, write-side. Sin conexión real con Bybit, sin uso de Railway; `execution_gateway` permanece abierto, no congelado.

- **Reauditoría post-corrección 3.72 — `ACEPTAR HITO 3.72`**, sin cambios de código — reauditoría focalizada, verificando exclusivamente los hallazgos de la corrección post-3.72 sin repetir la auditoría completa original. Confirmado: los cinco account-wide fields siguen fail-closed ante `""` (5/5) y aceptan `"0"` legítimo (4/4, sin over-tightening); `total_available_balance` documentado correctamente sin lenguaje de "buying power"; ausencia de USDT verificada como resultado legítimo sin fila sintética; negativos legítimos aceptados en 9/9 casos probados, márgenes negativos correctamente rechazados; duplicados de moneda preservados sin deduplicación; `result.list==1` confirmado como invariante Phoenix documentada honestamente (no garantía Bybit); no-atomicidad documentada en ADR-002 con los cuatro requisitos futuros explícitos; mutante de finitud reconfirmado absorbido por el contrato (defensa en profundidad, MENOR aceptado); response-close confirmado cerrado. Suite: 5125 passing sin alteración. Respuestas a las 10 preguntas obligatorias: política fail-closed aceptable (sí), incertidumbre de isolated margin correctamente documentada (sí), `total_available_balance` suficientemente acotado (sí), USDT ausente correctamente interpretado (sí), negativos protegidos (sí), duplicados protegidos (sí), `result.list==1` correctamente presentado como política Phoenix (sí), no-atomicidad suficientemente documentada (sí), primitiva sólida para observación (sí), sólida para Risk Engine directo (**no** — y correcto que no lo sea, el hito es observacional).

- **Hito 3.73 — Bybit Demo Instrument Metadata Read** (`instrument_metadata_contracts.py`, `instrument_metadata_reader.py`, `bybit_public_get_request_sender.py`, `bybit_public_get_api.py`, `bybit_instrument_metadata_response_interpreter.py`, `bybit_instrument_metadata_reader.py`, `bybit_demo_instrument_metadata_reader_factory.py`, `configured_bybit_demo_instrument_metadata_reader_factory.py`, `bybit_demo_instrument_metadata_reader_env_bootstrap.py`, `bybit_demo_instrument_metadata_query.py`), 347 tests nuevos — cuarta primitiva del read-side, junto a Positions/Open Orders/Wallet Balance Read, primera que toma un parámetro del llamador (`symbol`) y primera que usa un endpoint **público** (sin autenticación). **Diagnóstico previo contra documentación oficial de Bybit V5**: endpoint `GET /v5/market/instruments-info`, confirmado explícitamente *"Authentication: Not required"* — a diferencia de los tres endpoints anteriores, todos privados. Documentación de Demo Trading confirmó que los datos públicos son idénticos a mainnet, accesibles desde `api-demo.bybit.com` (mismo host que el resto de Phoenix, D-011, sin introducir un segundo host). **Decisión arquitectónica central del hito (sección dedicada del ADR-002, Decisión 6)**: auditoría explícita del GET stack existente antes de escribir código — `UrllibGetHttpTransport`/`HttpGetRequestExecutor` reutilizables sin cambios (genéricos, ninguna noción de autenticación, `headers={}` ya soportado); `BybitPrivateGetRequestSender`/`BybitPrivateGetApi` (firma HMAC vía `BybitAuthenticator`/`BybitHeaderBuilder`) **deliberadamente no reutilizados** — construirlos para un endpoint público habría exigido credenciales Demo ficticias sólo para satisfacer un constructor, acoplando en falso una lectura pública a secretos que nunca usa; se construyó en cambio una primitiva pública mínima y paralela (`BybitPublicGetRequestSender`, sin `authenticator` ni `header_builder`, headers vacíos; `BybitPublicGetApi`, mismo split que la privada, **reutilizando sin duplicar** `BybitResponseParser` — el envelope `retCode`/`retMsg`/`result`/`retExtInfo`/`time` es idéntico entre respuestas públicas y privadas). **Composición sin credenciales**: `create_configured_bybit_demo_instrument_metadata_reader(*, timeout_seconds)` deliberadamente no acepta `BybitDemoExecutionConfig`; el bootstrap de entorno lee únicamente `PHOENIX_HTTP_TIMEOUT_SECONDS` (variable ya existente, reutilizada — **cero variables `PHOENIX_*` nuevas**) mediante un loader mínimo propio, sin exigir `PHOENIX_BYBIT_DEMO_API_KEY`/`API_SECRET`/`RECV_WINDOW_MS` — verificado conductualmente de punta a punta: una consulta de metadata funciona con esas tres variables completamente ausentes del entorno, y el request HTTP real no contiene ningún header `X-BAPI-*`. **Identidad del instrumento — fail-closed inequívoco**: se consulta por `symbol` exacto (URL-encoded en la query string, sin whitelist local de símbolos conocidos, sin normalización de casing silenciosa); el interpreter exige `len(result.list) == 1` (0 elementos = símbolo inexistente, >1 = respuesta ambigua, ambos rechazados, nunca se toma "el primero") y además compara por valor exacto el `symbol` remoto devuelto contra el solicitado — un símbolo remoto distinto aborta la consulta en vez de servir metadata bajo una identidad no verificada. **Contrato `ExecutionInstrumentMetadata`** — 12 campos esenciales (`symbol`/`base_asset`/`quote_asset`/`settlement_asset`/`instrument_status`/`contract_type`/`tick_size`/`min_price`/`max_price`/`qty_step`/`min_order_qty`/`max_order_qty`/`server_time_ms`) y 5 accesorios (`max_market_order_qty`/`min_notional_value`/`min_leverage`/`max_leverage`/`leverage_step`, ninguno confirmado presente para todo instrumento linear — mismo patrón `leverage`/`unrealized_pnl` de Positions Read). `instrument_status`/`contract_type` preservados **sin traducir a un enum cerrado** — divergencia deliberada y justificada respecto de `side`/`order_type`/`status` en Positions/Open Orders Read: aquellos endpoints documentan un universo cerrado y acotado, mientras que `instruments-info` expone el ciclo de vida completo de cualquier instrumento y Bybit no documenta el enum completo de `status` — tratarlo como cerrado bloquearía la observación de estados legítimos no anticipados. `max_market_order_qty` (maxMktOrderQty) modelado explícitamente distinto de `max_order_qty` (maxOrderQty, límite de órdenes limit) y `min_notional_value` (price×qty) distinto de `min_order_qty` (cantidad de contratos) — ambas distinciones confirmadas con el ejemplo oficial de Bybit (valores distintos para el mismo instrumento) y protegidas con mutaciones dedicadas. **Leverage filter documentado extensamente como metadata no operativa** — `min_leverage`/`max_leverage`/`leverage_step` describen lo que Bybit permite técnicamente, nunca una recomendación operativa; ningún código de este hito calcula, configura o usa leverage a partir de estos valores, verificado explícitamente por inspección estática. **Fail-closed ante paginación** (mismo mecanismo que Positions/Open Orders, a diferencia de Wallet Balance donde el campo no existe en el esquema). `BybitInstrumentMetadataReader.query_instrument_metadata()` traduce todo fallo a `ExecutionInfrastructureError` ya existente, mismo principio que los tres readers anteriores; `TestNoCacheAcrossCalls` incluido desde el primer commit. **347 tests nuevos, suite 5125→5472 (+347 tests netos); 19/20 mutaciones especificadas verificadas y detectadas manualmente sobre el código real** (endpoint incorrecto, GET→POST, Demo→Mainnet, símbolo solicitado ignorado, símbolo remoto no validado, lista vacía aceptada, múltiples instrumentos, `tickSize`→float, `minNotionalValue` confundido con `minOrderQty`, `maxMktOrderQty` confundido con `maxOrderQty`, cursor ignorado, doble request, caché por instancia, timeout ignorado, response no cerrado, `ret_msg` filtrado, `except Exception` agregado, `server_time_ms` incorrecto) — cada mutación restaurada y verificada byte-idéntica contra el original; **1 mutación no aplicable** (leverage máximo usado como valor operativo): no existe, por diseño, ningún camino de código en este hito que use `max_leverage` operativamente, verificado por inspección estática. Sin conexión real con Bybit en ningún momento, sin órdenes creadas/canceladas/modificadas, sin tocar Railway, `phoenix_core`, Positions/Open Orders/Wallet Balance Read ni el write-side; `execution_gateway` permanece abierto, no congelado.

- **Corrección post-3.73 — hallazgos de la auditoría adversarial independiente** (`tests/test_execution_gateway_bybit_instrument_metadata_response_interpreter.py`), 5 tests nuevos — auditoría adversarial independiente (reconstruyó el pipeline sin confiar en el informe del implementador, reinyectó mutaciones propias) clasificó `CORREGIR HITO 3.73` con 1 hallazgo IMPORTANTE y 2 MENORES. **IMPORTANTE-1:** confirmado por inspección directa que el mapping `baseCoin`/`quoteCoin`/`settleCoin`→`base_asset`/`quote_asset`/`settlement_asset` en `bybit_instrument_metadata_response_interpreter.py` **ya era correcto en producción** — el hallazgo era de cobertura, no de comportamiento (mismo patrón que IMPORTANT-3 de la auditoría del Hito 3.70). Causa raíz: todos los fixtures que atravesaban el interpreter real usaban `quoteCoin==settleCoin=="USDT"` (valores idénticos), dejando invisible cualquier cruce entre esos dos campos específicos — la única prueba con valores distintos construía el contrato directamente, sin pasar por el interpreter. Corregido exclusivamente con `TestBaseQuoteSettlementIdentity` (5 tests): tres valores mutuamente distintos (`baseCoin="AAA"`, `quoteCoin="BBB"`, `settleCoin="CCC"`) atravesando siempre el interpreter real, con tres aserciones independientes por campo más un probe multi-field (filtros de precio/cantidad/leverage también mutuamente distintos, `symbol` deliberadamente mantenido en `"BTCUSDT"` para no inferir identidad desde el symbol). **Cero cambios de producción** — verificado byte-idéntico contra el commit auditado (`7981283`) tras cada una de las cinco mutaciones reinyectadas (`quote_asset`↔`settlement_asset`, `settlement_asset←quoteCoin`, `base_asset←quoteCoin`, `quote_asset←baseCoin`, `settlement_asset←baseCoin`), las cinco detectadas. **MENOR-2:** metadata remota excluida documentada en ADR-002 con marcado explícito DOCUMENTADO/INFERIDO por campo — `unifiedMarginTrade` (DOCUMENTADO: booleano de compatibilidad UTA, candidato a futura Order Validation), `riskParameters` (DOCUMENTADO como bandas de precio, pero la fórmula de aplicación de `priceLimitRatioX`/`Y` no está documentada con suficiente detalle para implementar ahora — candidato más fuerte, junto con `unifiedMarginTrade`, para un futuro hito), `postOnlyMaxOrderQty` (DOCUMENTADO explícitamente *deprecated* por Bybit — máxima confianza en la exclusión), `launchTime`/`isPreListing` (sin consumidor identificado). Ningún campo agregado al contrato. **MENOR-1** (`leverageFilter` no-Mapping tolerado como ausencia, asimetría ya documentada como deliberada) y **MENOR-3** (loader de timeout con `ValueError` crudo, idéntico al patrón ya aceptado desde el Hito 3.65) aceptados como deuda sin cambio, fuera de alcance de esta corrección. Suite completa re-ejecutada: 5477 passing en el estado final, 0 regresiones. **No se tocó ningún archivo productivo** — confirmado por `git diff --stat -- platform/` vacío; Positions/Open Orders/Wallet Balance Read, GET stack privado y público, `authenticator`, `header_builder`, `response_parser`, write-side, `phoenix_core`, `railway.toml`, `pyproject.toml` intactos. Sin conexión real con Bybit, sin uso de Railway; `execution_gateway` permanece abierto, no congelado. **Actualización posterior: Hito 3.73 — ACEPTADO** tras reauditoría adversarial independiente que confirmó ambos hallazgos (IMPORTANTE-1 e IMPORTANTE-2 originales) cerrados sin regresión — no se dispone en este repositorio de un registro contemporáneo adicional de esa reauditoría más allá de esta anotación (ver `docs/progress.md`, fila "Reauditoría post-corrección 3.73"); el contenido de esta corrección (hallazgos, tests, mutaciones, commit `b9c995d`) se preserva sin alterar.

- **Hito 3.74 — `ExchangeStateSnapshot`/`CompositeExchangeStateReader`** (`exchange_state_contracts.py`, `exchange_state_reader.py`, `composite_exchange_state_reader.py`, `bybit_demo_exchange_state_reader_factory.py`, `configured_bybit_demo_exchange_state_reader_factory.py`, `bybit_demo_exchange_state_reader_env_bootstrap.py`, `bybit_demo_exchange_state_query.py`), 173 tests nuevos — primer primitivo de agregación del read-side: combina las tres primitives account-wide ya aceptadas (Positions 3.70, Open Orders 3.71, Wallet Balance 3.72) en un `ExchangeStateSnapshot` a través de un quinto Port de lectura, `ExchangeStateReader`. **No es, y explícitamente no pretende ser, una fotografía atómica**: cada sub-snapshot proviene de un request HTTP independiente; el agregado preserva esa no-atomicidad en vez de esconderla. **Diagnóstico previo:** los cuatro contratos ya aceptados fueron releídos por completo antes de escribir código — los tres incluidos ya preservaban `server_time_ms: int` remoto (sourced de `response.time_ms`), sin necesitar ninguna extensión. **STOP arquitectónico explícito, tal como el hito lo exigía ante ambigüedad real:** el diagnóstico identificó que Instrument Metadata (Hito 3.73) es la única primitiva por-símbolo (exige `symbol: str`) entre las cuatro aceptadas — decidir si/cómo incluirla en el agregado tenía más de una solución razonable y afectaba la forma del contrato público. Se presentaron tres alternativas mediante `AskUserQuestion` (A: agregado sin metadata; B: parámetro `symbols` explícito; C: derivación automática desde posiciones/órdenes) sin elegir arbitrariamente. **El usuario eligió la Opción A.** `ExchangeStateSnapshot` agrega exactamente `positions`/`open_orders`/`wallet_balance`/`observation_window` — sin campo de metadata, sin `server_time_ms` de nivel superior (evita sugerir falsamente un instante único). **`ObservationWindow`** (`earliest_remote_time_ms`/`latest_remote_time_ms`/`remote_time_span_ms`, los tres campos almacenados explícitamente) se computa exclusivamente de los tres `server_time_ms` remotos vía `min()`/`max()`, nunca de `time.time()` ni de ningún reloj local; **deliberadamente sin ningún campo ni política de "ventana máxima aceptable"** — mide y expone el drift observado, nunca decide una tolerancia (instrucción explícita del hito, evitando inventar la política que un futuro Reconciliation Engine debe decidir); `ExchangeStateSnapshot.__post_init__` valida por consistencia cruzada que la ventana recibida coincida con el `min()`/`max()` recién recalculado de los tres sub-snapshots, rechazando una ventana mezclada con otra ronda. **Orden de lectura fijo y secuencial** (Positions → Open Orders → Wallet Balance, el mismo orden en que las tres fueron aceptadas) — documentado explícitamente como arbitrario-pero-fijo, no una garantía de cercanía temporal; sin concurrencia, por no existir necesidad demostrada. **Fail-closed ante fallo parcial logrado sin ningún `try`/`except` nuevo:** `CompositeExchangeStateReader.query_exchange_state()` no atrapa ninguna excepción de los tres sub-readers — se propaga sin envolver y los readers restantes de la ronda nunca se llaman; verificado que esto es coherente con el error model existente antes de asumirlo (identidad de la excepción original preservada, excepciones internas `TypeError`/`ValueError`/`AssertionError`/`RuntimeError`/`AttributeError` siguen distinguibles de `ExecutionInfrastructureError`); cero campos `Optional`/con default en `ExchangeStateSnapshot`/`ObservationWindow`, verificado con test dedicado, para no poder ocultar un fallo de infraestructura tras un campo ausente. **Exactamente una llamada por reader por ronda, sin caché entre rondas en la misma instancia** — misma garantía conductual ya exigida a `PositionsReader` desde la corrección post-3.70, extendida al agregador con tests de spy dedicados. **Composición aditiva, sin refactor transversal:** `configured_bybit_demo_exchange_state_reader_factory.py` reutiliza sin modificar las tres factories configuradas ya aceptadas — deuda documentada y no bloqueante: tres stacks GET privados independientes construidos por ronda, no compartidos entre las tres primitives (resolverlo habría exigido tocar las tres factories ya aceptadas, el refactor transversal que el hito instruía evitar sin necesidad demostrada). **Batería de mutación:** 14/16 mutaciones aplicables, las 14 detectadas — 2/16 (M4/M13, ambas sobre Instrument Metadata) no aplicables por diseño, documentado con verificación de que no existe código que mutar (Instrument Metadata fuera del alcance del agregado por la Opción A, confirmado por auditoría de imports vía AST). **Hallazgo de la propia batería, corregido en el mismo hito:** la mutación de "mezcla de rondas" (M14 — el agregador retiene el `latest` más grande visto entre rondas en vez de recalcularlo desde cero) sólo era detectada estructuralmente (un test de atributos de instancia) porque el test de dos rondas ya existente usaba timestamps crecientes entre rondas, donde "mezclar" coincide por accidente con el valor correcto; se agregó `test_second_round_with_smaller_timestamps_never_blends_with_first_round` (ronda 1 con timestamps grandes, ronda 2 con timestamps más pequeños) que la detecta conductualmente por el valor incorrecto, cerrando el hueco de cobertura antes de continuar. Suite completa re-ejecutada: 5650 passing en el estado final (5477→5650, incluye el test de cierre de cobertura), 0 regresiones. **No se tocó** ninguna de las cuatro primitives 3.70-3.73 (contratos, Ports, adapters, interpreters, factories, bootstraps, queries — sólo `__init__.py` extendido aditivamente con 8 exports nuevos), los stacks GET privado y público, `authenticator`/`header_builder`/`response_parser`/`url_builder`, write-side, `phoenix_core`, `railway.toml`, `pyproject.toml`. Sin conexión real con Bybit, sin ninguna orden creada/cancelada/modificada, sin uso de Railway. **Nota de estado de la documentación (vigente al momento de este commit):** `docs/progress.md` registraba el Hito 3.73 como pendiente de reauditoría final (último commit relevante: `612a90b`, corrección post-3.73) — no se encontró evidencia documental de una reauditoría/aceptación formal posterior; ese estado se preservó sin alterar entonces, por estar fuera del alcance de este hito. **Actualización posterior:** el Hito 3.73 fue confirmado ACEPTADO tras esa reauditoría (ver bullet de la corrección post-3.73, arriba, y `docs/progress.md`).

- **Corrección post-3.74 — hallazgo IMPORTANTE-1 de la auditoría adversarial independiente** (`tests/test_execution_gateway_bybit_demo_exchange_state_reader_composition.py`), 9 tests nuevos — la auditoría verificó conductualmente, con sondas propias (mapping de entorno espía/mutante, monkeypatch de constantes, extracción de atributos del grafo de objetos real), que producción **ya era correcta**: `bootstrap_bybit_demo_exchange_state_reader_from_env` carga `BybitDemoExecutionConfig` una única vez (`os.environ` leído exactamente una vez por clave, incluso con un entorno deliberadamente mutante entre lecturas) y ese mismo objeto de config se pasa a las tres factories configuradas ya aceptadas (`create_configured_bybit_demo_positions_reader`/`..._open_orders_reader`/`..._wallet_balance_reader`), por lo que los tres sub-readers privados autentican siempre con la misma `api_key`/`api_secret`/`recv_window_ms`/`timeout_seconds`/base URL Demo. El hallazgo era exclusivamente de **cobertura, no de comportamiento** (mismo patrón que IMPORTANTE-1 de la corrección post-3.73): un mutante que hiciera que Wallet Balance autenticara contra una API key, secret, `recv_window_ms`, `timeout_seconds` o base URL **distintos** de Positions/Open Orders sobrevivía los 173 tests del hito. **Cero cambios de producción** — verificado byte-idéntico contra `f320040` (`git diff --quiet f320040 -- platform/` limpio antes del commit). Cerrado exclusivamente con `TestConfigCoherenceAcrossSubReaders` (6 tests nuevos en el archivo de composición): un helper `_config_fingerprint(reader)` extrae, navegando el grafo de objetos real construido por la factory (`reader._private_get_api._sender._authenticator._credentials.api_key/api_secret`, `.._recv_window_ms`, `.._request_executor._timeout_seconds`, `reader._url_builder._base_url`) — nunca de `inspect.getsource` ni de mocks —, los cinco valores efectivos de cada uno de los tres sub-readers, y asevera que coinciden entre Positions/Open Orders/Wallet Balance; más 2 tests nuevos en `TestBootstrap` (lectura de `os.environ` exactamente una vez por clave vía mapping espía; coherencia extremo-a-extremo desde la función pública de bootstrap con un entorno distintivo) y 1 test de control negativo (confirma que el fingerprint distingue configuraciones realmente distintas, no es una tautología). **8/8 mutaciones especificadas por la auditoría verificadas y detectadas**: Wallet con `api_key`/`api_secret`/`recv_window_ms`/`timeout_seconds` divergente (mutando temporalmente `configured_bybit_demo_exchange_state_reader_factory.py` vía `dataclasses.replace(config, ...)` antes de pasar a la sub-factory correspondiente, restaurado y verificado byte-idéntico tras cada mutación), Open Orders con `api_key` divergente, Positions con `api_key` divergente, y relectura independiente de config para Wallet en el bootstrap (mutando temporalmente `bybit_demo_exchange_state_reader_env_bootstrap.py`, restaurado igual); la mutación de base URL divergente se verificó por **monkeypatch en memoria** sobre la constante `_BYBIT_DEMO_BASE_URL` del factory de nivel inferior de Wallet Balance (`bybit_demo_wallet_balance_reader_factory.py`, Hito 3.72) — deliberadamente sin escribir en disco, para no tocar ni siquiera transitoriamente el archivo de una primitiva ya aceptada. Suite completa re-ejecutada: 5659 passing en el estado final (5650→5659), 0 regresiones. Corrección MENOR adicional: el conteo histórico del Hito 3.74 decía "172 tests nuevos" en `progress.md`/`handoff.md`; el delta real de la suite era 173 (5650−5477) — corregido en ambos documentos. **No se tocó** ninguna primitiva 3.70-3.73, ningún GET stack, write-side, `phoenix_core`, `railway.toml`, `pyproject.toml`; los únicos archivos productivos del Hito 3.74 permanecen byte-idénticos a `f320040`. Sin conexión real con Bybit, sin uso de Railway. **Hito 3.74 corregido — no declarado aceptado — listo para reauditoría adversarial final.**

- ADR-001 — `ExecutionGateway` convertido en Port real del dominio; `BybitExecutionGateway` convertido en Adapter (`bybit_gateway.py`, único archivo de producción modificado), 39 tests nuevos — origen: la Auditoría Retrospectiva A detectó que `BybitExecutionGateway.execute` delegaba directamente `self._client.place_order(request)` sin traducir, por lo que en producción sólo aceptaba `BybitCreateOrderRequest`/devolvía `BybitCreateOrderResult`, incumpliendo la anotación pública `ExecutionRequest -> ExecutionResult` del Protocol `ExecutionGateway` (`isinstance` daba `True` pese al incumplimiento); implementación: `BybitExecutionGateway` agrega dos métodos privados, `_to_bybit_request` y `_to_execution_result` — toda la traducción vive exclusivamente ahí, ningún otro módulo conoce tipos `Bybit*`; mapeo `ExecutionRequest → BybitCreateOrderRequest`: `side`/`order_type` capitalizados vía diccionario fijo, `quantity`/`price` convertidos con `Decimal(str(valor))` (evita artefactos de precisión binaria), `order_link_id = order_id` (el id de dominio se reutiliza como clave de idempotencia de Bybit), `time_in_force="GTC"` y `reduce_only=False` fijados por el adaptador (el dominio no modela esos conceptos); mapeo `BybitCreateOrderResult → ExecutionResult`: `order_id` conserva la identidad de dominio original (no el id que genera Bybit), `exchange_order_id` transporta el id de Bybit, `status="accepted"` (crear la orden sólo confirma aceptación, no fill); las excepciones (`BybitApiError`, errores de transporte/HTTP) siguen propagando sin envolver, sin `try/except` nuevo; no se modificó ninguna factory inferior ni el Composition Root — la firma de `BybitExecutionGateway.__init__` no cambió, por lo que ni `create_bybit_demo_execution_gateway` ni `create_configured_bybit_demo_execution_gateway` requirieron cambios; `ExecutionGateway` Protocol y `contracts.py` sin modificar (ya eran correctos; el defecto estaba sólo en el adaptador); pureza de dominio verificada con archivo dedicado `tests/test_execution_gateway_domain_purity.py` (12 tests): `gateway.py`/`contracts.py` no importan ni referencian ningún nombre `Bybit*`; las tres implementaciones de `ExecutionGateway` (`DryRunExecutionGateway`, `FakeExecutionGateway`, `BybitExecutionGateway`) vuelven a ser intercambiables bajo el mismo Protocol; tests migrados (dobles y aserciones ajustados al contrato de dominio, sin cambiar lo que verifican): `test_execution_gateway_bybit_gateway.py` (reescrito completo, 42 tests), `test_execution_gateway_create_order_integration.py` (Sección B reescrita, Sección A intacta, 96 tests), `test_execution_gateway_bybit_demo_execution_gateway_factory.py` (`TestIntegratedFlow` migrada, `_make_request` obsoleta eliminada, 65 tests), `test_execution_gateway_factory.py` (stubs `_ValidClient`/`AnotherClient` corregidos al contrato real de `BybitDemoClient.place_order`, 20 tests); decisión registrada en `docs/decisions.md` como ADR-001; no se agregaron dependencias, no se lee entorno, no hay conexión real con Bybit Demo

- ADR-001A — Camino de error del Port desacoplado de Bybit — origen: la auditoría independiente del ADR-001 (`CORREGIR HITO 3.64 (ADR-001)`) detectó que, aunque el camino feliz ya traducía correctamente, `BybitApiError` seguía atravesando `BybitExecutionGateway.execute()` sin traducir, y que las tres implementaciones de `ExecutionGateway` (`DryRun`, `Fake`, `Bybit`) dejaban de ser sustituibles (LSP) en el camino de rechazo; implementación, exclusivamente en `bybit_gateway.py` (único archivo de producción modificado además de la creación de la nueva excepción y su export en `__init__.py`): `execute()` ahora envuelve `self._client.place_order(bybit_request)` en un único `try/except` con dos ramas — `except BybitApiError as error: return self._to_rejected_result(...)` traduce el rechazo de negocio a `ExecutionResult(order_id=request.order_id, status="rejected", error_message=error.ret_msg)` (nuevo método privado `_to_rejected_result`, sin excepción, coherente con el vocabulario de rechazo que `ExecutionResult` ya modelaba desde `contracts.py` pero que el adaptador nunca producía); `except Exception as error: raise ExecutionInfrastructureError(message=str(error)) from error` traduce cualquier otra falla (transporte, DNS, socket, bugs) a la nueva excepción de dominio, preservando el original vía encadenamiento real de Python (`__cause__`, no identidad directa como antes) — la traducción de la request (`_to_bybit_request`) permanece **fuera** del `try`, así que los `ValueError`/`TypeError` de validación de dominio (p.ej. `order_link_id` > 36 caracteres) siguen propagando sin cambios, no se consideran "error de infraestructura"; nueva clase pública `ExecutionInfrastructureError` (`execution_infrastructure_error.py`) — `Exception` directa, constructor keyword-only `message: str` con la misma validación de tipo/vacío que el resto del núcleo, superficie mínima (un solo atributo público `message`, sin `ret_code`, sin `retryable`, sin `original_error`), módulo sin ninguna referencia a `Bybit*` (verificado); no se tocó `BybitDemoClient`, el parser, el transport, el authenticator, el payload builder, el serializer ni ninguna factory inferior; el Composition Root (`create_configured_bybit_demo_execution_gateway`, `create_bybit_demo_execution_gateway`) no requirió cambios — la firma de `BybitExecutionGateway.__init__` no cambió; tests eliminados por establecer como contrato público el comportamiento incorrecto: `test_propagates_bybit_api_error` y las aserciones equivalentes en `test_execution_gateway_bybit_gateway.py`, `test_execution_gateway_create_order_integration.py` (`TestGatewayApiRejection`) y `test_execution_gateway_bybit_demo_execution_gateway_factory.py`; reemplazados por clases que verifican la traducción real (`TestBusinessRejectionTranslation` nueva en `test_execution_gateway_bybit_gateway.py`, `TestGatewayApiRejection`/`TestGatewayInfrastructureError` reescritas en el archivo de integración); nuevo archivo `tests/test_execution_gateway_execution_infrastructure_error.py` (contrato completo de la excepción: construcción, validación, mensaje, encadenamiento vía `from`, superficie mínima, pureza de dominio); `tests/test_execution_gateway_domain_purity.py` extendido con `TestErrorPathIsAlsoDomainOnly` (5 tests): `BybitApiError` no se referencia en `gateway.py`/`contracts.py`, el módulo de la nueva excepción no referencia `Bybit*`, y dos tests conductuales confirman que ni el rechazo ni la falla de infraestructura cruzan `execute()` en su forma Bybit original; **la sección A del archivo de integración (`BybitDemoClient.create_order()` directo) no se modificó** — `BybitApiError` sigue siendo el contrato correcto y esperado en esa capa interna, sólo el adaptador de dominio lo traduce; no se agregaron dependencias, no se lee entorno, no hay conexión real con Bybit Demo

- Core Hardening Pack A — cierre de la Auditoría Retrospectiva A, origen: auditoría independiente clasificó `CORREGIR AUDITORÍA A` señalando que todo `ret_code != 0` se traducía a `rejected` (incluidos autenticación, firma, rate limit, servidor), que `except Exception` enmascaraba defectos de programación, que `message=str(error)` filtraba texto sin sanear, y que `HttpRequest`/`BybitResponse` (mutabilidad profunda), `repr` (firma/API key expuestas), `GatewayConfig` (sin validación de tipos) y `factory.py` (`BybitDemoClient` en una firma que devuelve el Port) seguían sin resolver desde la Auditoría A original; implementación en 12 partes (A–L):
  - **A/B/C** (`bybit_gateway.py`) — clasificador privado `_ORDER_REJECTION_RET_CODES = frozenset({10001, 110003, 110004, 110007})`: sólo estos códigos producen `ExecutionResult(status="rejected", error_message=error.ret_msg)`; cualquier otro `ret_code` (autenticación, firma, rate limit, timestamp, servidor, o **cualquier código desconocido**) se trata fail-closed como `ExecutionInfrastructureError`; `except Exception` eliminado — sólo se captura `(OSError, json.JSONDecodeError)` (los únicos tipos concretos que la cadena real de transporte puede producir); `TypeError`/`ValueError` genérico/`AttributeError`/`KeyError`/`AssertionError`/`RuntimeError` propagan sin envolver; mensaje de infraestructura es la constante fija `"Bybit execution infrastructure failure"` — nunca `str(error)`, nunca `ret_msg`, nunca `ret_code`; el detalle original vive sólo en `__cause__`
  - **D** (`http_request.py`, + cambio imprescindible en `urllib_http_transport.py`) — `headers` envuelto en `MappingProxyType` tras copia defensiva; anotación pública `Mapping[str, str]`; `UrllibHttpTransport.post` valida `isinstance(headers, Mapping)` en lugar de `dict` (imprescindible: un `MappingProxyType` no es `dict`, y de lo contrario la cadena real se rompía)
  - **E** (`bybit_response.py`) — `_deep_freeze` recursivo: `dict`→`MappingProxyType`, `list`→`tuple`, `set`→`frozenset`; compatible con `BybitCreateOrderResponseInterpreter` sin cambios
  - **F** (`http_request.py`, `bybit_authenticator.py`) — `BybitAuthentication.signature` con `field(repr=False)`; `HttpRequest.__repr__` propio: URL visible, sólo nombres de headers (nunca valores), body oculto
  - **G** (`config.py`) — `GatewayConfig` con validación estricta: `environment` `str` no vacío/whitespace sin `strip`, `dry_run` `bool` exacto, `timeout_seconds` `int` con `bool` rechazado — mismo patrón que el resto del núcleo
  - **H** (`factory.py`) — `create_execution_gateway(config: GatewayConfig) -> ExecutionGateway`, parámetro `client` eliminado; `dry_run=False` lanza `ValueError("Live execution requires a dedicated composition root for the selected adapter.")` sin nombrar ningún exchange; `create_configured_bybit_demo_execution_gateway` sigue siendo el único composition root para ejecución real
  - **I** (`bybit_gateway.py`, + cambio imprescindible en `bybit_create_order_payload_builder.py`) — `_to_plain_decimal` privada rechaza `NaN`/`+inf`/`-inf` con `ExecutionRequestNotSupportedError` antes de construir el `Decimal`; verificado matemáticamente que ninguna estrategia de construcción de `Decimal` evita notación científica en `str()` para magnitudes extremas (`Decimal.__str__` la fuerza por especificación cuando el exponente ajustado es < -6 o positivo) — el único punto real donde se puede garantizar salida plana es la serialización; `BybitCreateOrderPayloadBuilder` cambia `str(request.quantity)`/`str(request.price)` por `format(valor, "f")`, que sí preserva precisión exacta sin notación exponencial (verificado: `1e-8 → "0.00000001"`, `1e16 → "10000000000000000"`)
  - **J** (`bybit_gateway.py`) — `_to_execution_result` compara `result.order_link_id` contra `request.order_id`; discordancia lanza `ExecutionInfrastructureError` (mensaje seguro) en lugar de confirmar una orden sin identidad verificada
  - **K** (`bybit_gateway.py`) — `_to_bybit_request` valida `len(order_id) <= 36` **antes** de construir `BybitCreateOrderRequest`, evitando que el mensaje `"order_link_id must be at most 36 characters"` escape al dominio; nueva excepción `ExecutionRequestNotSupportedError(message="Execution request cannot be represented by the selected adapter")`, reutilizada también por la Parte I (misma semántica: el adaptador elegido no puede representar esta solicitud de dominio válida)
  - **L** (`tests/test_execution_gateway_domain_purity.py`) — regla de pureza aplicada simétricamente a `gateway.py`, `contracts.py`, `execution_infrastructure_error.py`, `execution_request_not_supported_error.py` y `factory.py` por igual; cierra la asimetría original que dejaba pasar `BybitDemoClient` en `factory.py`
  - archivos de producción tocados: `bybit_gateway.py`, `http_request.py`, `bybit_response.py`, `bybit_authenticator.py`, `config.py`, `factory.py`, `__init__.py`, y por necesidad estrictamente demostrada `urllib_http_transport.py` y `bybit_create_order_payload_builder.py`; archivo nuevo: `execution_request_not_supported_error.py`; `execution_infrastructure_error.py` no requirió cambios de código
  - no se tocó `BybitDemoClient`, el authenticator, el request builder, el sender, el serializer ni ninguna factory inferior ni el Composition Root; `phoenix_core` intacto; sin dependencias nuevas
  - batería de mutación aplicada sobre los 10 cambios de comportamiento (clasificador invertido, unknown-como-rechazo, catch-all restaurado, `str(error)` restaurado, chequeo de correlación salteado, headers mutables de nuevo, `dry_run` laxo, `BybitDemoClient` reintroducido en la factory, notación científica reintroducida) — las 10 mutaciones fueron detectadas por la suite
  - decisión registrada en `docs/decisions.md`; deuda documentada y explícitamente fuera de alcance: duplicación de la constante de longitud 36 entre tres archivos, `time_in_force`/`reduce_only` fijos en el adaptador, doble validación preexistente de `recv_window_ms`/`timeout_seconds` (hitos 3.51/3.52/3.59/3.60); no se lee entorno, no hay conexión real con Bybit Demo, no se implementaron retries/backoff/logging/cancelación/consultas/posiciones

- Corrección final de Auditoría A — Respuestas remotas no interpretables, origen: la reauditoría del Core Hardening Pack A demostró, sustituyendo únicamente `urlopen` a través de la composición productiva real, que 6 de 7 respuestas malformadas ya recibidas de la red (body no-UTF8, JSON malformado, claves obligatorias ausentes, tipos inválidos) escapaban como excepción cruda (`UnicodeDecodeError`, `KeyError`, `TypeError`) en lugar de `ExecutionInfrastructureError` — exactamente la ambigüedad operacional que esa excepción existe para representar; nueva excepción interna `BybitResponseProcessingError` (`bybit_response_processing_error.py`, mismo patrón que `BybitApiError`/`ExecutionInfrastructureError`: constructor keyword-only `message: str`, validación de tipo/vacío) — representa exclusivamente "la respuesta remota fue recibida pero no pudo decodificarse, parsearse o interpretarse"; **no forma parte del dominio y nunca cruza `execute()`** (verificado en `tests/test_execution_gateway_domain_purity.py`); traducción ubicada en la frontera inferior, cada componente traduce sólo los fallos que sabe que provienen de una respuesta remota:
  - `bybit_private_api.py` — `request()` envuelve `self._sender.send(...)` en `try/except UnicodeDecodeError`, único punto donde el transporte genérico (`UrllibHttpTransport`, sin tocar, sigue siendo agnóstico de exchange) entrega el control a la interpretación específica de Bybit
  - `bybit_response_parser.py` — `parse()` envuelve `serializer.loads(...)` en `try/except JSONDecodeError` (import `from json import JSONDecodeError`, no `import json`, para preservar el invariante "no importa el módulo json completo"); la raíz no-dict se rechaza directamente con `BybitResponseProcessingError`; la construcción de `BybitResponse(...)` se envuelve en `try/except (KeyError, TypeError, ValueError)` — cubre claves ausentes (`retCode`/`retMsg`/`result`/`retExtInfo`/`time`) y tipos inválidos en un único punto
  - `bybit_create_order_response_interpreter.py` — `result` no-Mapping y `orderId`/`orderLinkId` ausentes se rechazan directamente con `BybitResponseProcessingError`; la construcción de `BybitCreateOrderResult(...)` se envuelve en `try/except (TypeError, ValueError)` — eliminó los `isinstance` manuales redundantes con las invariantes que el value object ya aplica (y cerró una omisión: antes no se validaba longitud/vacío de los IDs devueltos por Bybit)
  - `bybit_gateway.py` — `_TRANSPORT_FAILURES` pasa de `(OSError, json.JSONDecodeError)` a `(OSError, BybitResponseProcessingError)`; el `import json` se elimina del gateway (ya no lo necesita, la traducción de JSON ocurre una capa más abajo); **sigue sin existir `except Exception`/`except BaseException`** en ningún punto
  - `http_request.py` — corrección de la inconsistencia contractual detectada en la reauditoría (H2): `isinstance(self.headers, dict)` → `isinstance(self.headers, Mapping)`, cerrando el round-trip roto (`HttpRequest(headers=otro_request.headers)` fallaba porque `MappingProxyType` no es `dict`); mensaje de error actualizado a "headers must be a Mapping"; copia defensiva, `MappingProxyType`, validación de claves/valores y `repr` seguro se mantienen sin cambios
  - errores internos de programación (`TypeError`, `ValueError` genérico, `AttributeError`, `KeyError`, `AssertionError`, `RuntimeError`, `ZeroDivisionError` no originados en una respuesta remota) siguen propagando crudos sin envolver — verificado con dobles que inyectan el bug directamente en la capa de `BybitPrivateApi`, sin pasar por una respuesta real
  - todos los mensajes (`BybitResponseProcessingError` y `ExecutionInfrastructureError`) usan constantes fijas, nunca `str(error)`; el detalle técnico completo (incluidos los bytes crudos de un body no-UTF8) queda exclusivamente en la cadena de `__cause__`
  - tests nuevos: `tests/test_execution_gateway_bybit_response_processing_error.py` (contrato completo de la excepción, 25 tests); `tests/test_execution_gateway_create_order_integration.py` — nueva sección B.6 `TestMalformedRemoteResponses`, integración productiva completa con `create_configured_bybit_demo_execution_gateway` sustituyendo únicamente `urllib.request.urlopen`, cubre los 11 casos malformados + 1 de control, verificando tipo, mensaje seguro, ausencia de fuga de body/vocabulario Bybit y `__cause__` no nulo para cada uno; nueva sección B.7 `TestInternalBugsPropagateUnwrappedIntegration` (bugs internos crudos vía doble en la frontera de `BybitPrivateApi`); `tests/test_execution_gateway_bybit_private_api.py` extendido con `TestUnicodeDecodeErrorTranslation` (5 tests, incluida la verificación explícita de que el mensaje no es `str(error)`); `tests/test_execution_gateway_http_request.py` extendido con `TestHeadersAcceptsAnyMapping` (dict, `MappingProxyType`, `Mapping` custom, round-trip, inmutabilidad del resultado); tests existentes de `bybit_response_parser`, `bybit_response_parser_factory` y `bybit_create_order_response_interpreter` migrados de `TypeError`/`ValueError`/`KeyError` a `BybitResponseProcessingError`
  - batería de mutación (8 escenarios exigidos): `UnicodeDecodeError` escapando crudo, `KeyError` escapando crudo, `TypeError` remoto escapando crudo, `except Exception` reintroducido, `RuntimeError` envuelto incorrectamente, mensaje copiando el body remoto, `HttpRequest` volviendo a exigir `dict` — **8/8 detectadas**; la mutación de mensaje-inseguro reveló en el primer intento un hueco real de cobertura en `bybit_private_api.py` (ningún test verificaba que su propio mensaje no fuera `str(error)`), cerrado agregando la prueba correspondiente antes de commitear
  - no se modificó `phoenix_core`, `credentials.py`, `standard_bybit_authenticator.py`, `bybit_create_order_payload_builder.py`, `config.py` ni `configured_bybit_demo_execution_gateway_factory.py`; sin dependencias nuevas, sin lectura de entorno, sin conexión real con Bybit Demo
  - deudas menores aceptadas y explícitamente fuera de alcance de esta corrección: hash de `HttpRequest` (sigue inhashable, `MappingProxyType` envuelve un `dict`), estructuras cíclicas en `BybitResponse` (`RecursionError`, inalcanzable por el camino real), constante de longitud 36 duplicada (ahora en tres archivos)

**Tests totales:** 4812 passing

**Próximo hito:** por definir.


---

## Componente anterior: `phoenix_core` (CONGELADO en v0.1.0)

### Módulos existentes en `platform/phoenix_core/`

| Módulo | Clase/Función principal | Estado |
|--------|------------------------|--------|
| `__init__.py` | API pública con `__all__` | ✅ congelado |
| `ids.py` | `bot_id`, `signal_id`, `order_id`, `trade_id`, `event_id`, `portfolio_id`, `is_valid` | ✅ congelado |
| `config.py` | `Config`, `get_config()` | ✅ congelado |
| `events.py` | `Event` | ✅ congelado |
| `signals.py` | `Signal` | ✅ congelado |
| `orders.py` | `Order` | ✅ congelado |
| `trades.py` | `Trade` | ✅ congelado |
| `portfolio.py` | `Portfolio` | ✅ congelado |

### Tests en `tests/`

```
tests/test_import.py       — importación y API pública
tests/test_ids.py          — generación y validación de IDs
tests/test_config.py       — configuración por defecto
tests/test_events.py       — contrato de eventos
tests/test_signals.py      — contrato de señales
tests/test_orders.py       — contrato de órdenes (market/limit)
tests/test_trades.py       — contrato de operaciones
tests/test_portfolio.py    — contrato de portfolios
```

---

## Cómo correr los tests

```bash
cd /Users/jose/phoenix-platform
python3 -m pytest tests/ -v
```

---

## Qué falta construir

**Nota de vigencia:** esta sección quedó desactualizada durante un tiempo (describía la Fase 3 como "no definida todavía" mucho después de que ya estuviera extensamente implementada — ver `## Componente en curso` arriba y `docs/progress.md`). Corregida en el Hito 3.75. Ver siempre `docs/architecture.md` §12 para la frontera arquitectónica siguiente, no esta lista.

Estado real: la Fase 3 (`execution_gateway`) ya tiene write-side (`ExecutionGateway`/`BybitExecutionGateway`, tested-only, nunca validado contra Bybit real), cinco Ports de lectura completos y aceptados (Positions/Open Orders/Wallet Balance/Instrument Metadata/`ExchangeStateSnapshot`, Hitos 3.70-3.74), el lenguaje de dominio `ExpectedExecutionState` (Hito 3.76, contratos puros, sin Port — **ACEPTADO**), y `reconcile_execution_state` — Reconciliation Engine V1, Detection & Classification puro (Hito 3.77, corregido tras auditoría adversarial independiente — `CORREGIR` cerrado con fail-closed ante identidades observadas duplicadas, cobertura de `ObservationWindow`, y documentación de la coexistencia `OrderTypeMismatch`/`OrderPriceMismatch` — pendiente de reauditoría adversarial final; ver `docs/architecture.md` §14 para el alcance y las semánticas decididas). La siguiente frontera arquitectónica mayor, tras la aceptación de 3.77, queda por definir explícitamente: drift-tolerance policy, stale-round rejection, identidad de cuenta simétrica, y si el motor completo incluirá reparación. `bots/`/`scripts/` siguen vacíos — ningún bot vive todavía en este repositorio.

---

## Qué NO debe modificarse

- `platform/phoenix_core/` — está congelado en `v0.1.0`. No agregar lógica de trading.
- Los tests existentes deben seguir pasando en cada hito.
- `pyproject.toml` — no cambiar `requires-python` ni la configuración de `setuptools` sin documentar en `decisions.md`.
- El tag `v0.1.0` — no mover ni eliminar.

---

## Estructura del repo

**Nota de vigencia:** el árbol de abajo y el conteo de tests reflejaban únicamente el estado post-`phoenix_core` (Fase 2). Corregido en el Hito 3.75 — ver `docs/architecture.md` §3 para el mapa de componentes completo y actualizado.

```
phoenix-platform/
├── AGENTS.md            ← manual operativo para cualquier agente (Hito 3.75)
├── docs/
│   ├── architecture.md ← mapa del sistema actual (Hito 3.75)
│   ├── backlog.md      ← ideas pendientes de decisión
│   ├── decisions.md    ← decisiones de arquitectura
│   ├── handoff.md      ← este archivo
│   ├── progress.md     ← estado e hitos
│   └── components/     ← cierres de componentes grandes (phoenix-core.md, ...)
├── platform/
│   ├── phoenix_core/       ← núcleo congelado v0.1.0
│   └── execution_gateway/  ← write-side + 5 Ports de lectura (Fase 3, abierto)
├── bots/               ← vacío (sin bots todavía)
├── scripts/            ← vacío
├── tests/              ← 5659 tests, todos passing (111 archivos)
├── pyproject.toml
├── railway.toml
├── README.md
└── LICENSE
```

---

## Railway

**Nota de vigencia:** esta sección describía el estado inicial del proyecto Railway (Fase 1, sin servicios). Corregida en el Hito 3.75 — ver `docs/decisions.md` D-014 y la sección `## Estado al ...` arriba para el estado real y las decisiones de región/`PYTHONPATH`.

- **Proyecto:** `phoenix-platform`
- **ID:** `07fffce4-ec38-463b-a0a0-6a15fe640134`
- **Workspace:** `garciarjosefina's Projects`
- **Estado actual:** sin servicios activos. El único servicio jamás creado, `phoenix-smoke-demo` (configurado y validado exitosamente contra Bybit Demo en el Hito 3.68), fue **eliminado manualmente** por el usuario el 2026-08-12 — no existe riesgo de autodeploy desde `main` mientras esto siga así. `railway.toml` permanece en el repo como configuración de referencia (`buildCommand`/`startCommand`/`restartPolicyType = "NEVER"`) para un futuro redeploy manual; recrear el servicio exclusivamente desde el repo, sin agregar `PYTHONPATH=/app/platform` manualmente en el dashboard, reproducirá el `ModuleNotFoundError` documentado en D-014. Región obligatoria para cualquier servicio conectado a Bybit: EU West (Amsterdam, `europe-west4`) — ver D-014.
- **CLI:** `railway status` desde `/Users/jose/phoenix-platform`
